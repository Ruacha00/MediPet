# 复合交付、知识召回与回答效果

2026-09-18，起点main `bf4f353`。本轮为用户授权的产品修复和专项评测；原报告保留，新报告均未接受为基线。

## 修改与诊断

- 复合任务：补齐号源/材料表达，避免明确两项需求被低置信度OTHER整体澄清。首次模型请求分别指定只读号源与清单工具，保持三轮上限；儿童/首次类型依据本轮明确要求核对，实际覆盖值记入effective_input。
- 拒绝查号在修改选择列表之前拦截；当前主体与此前提及的孩子区分，不由patient_id或家属关系推断年龄。
- 知识召回：保持原向量集合，加入中文双字/英文词BM25和有限候选RRF融合。重排由截取整个JSON前200字符改为标题与正文分别限长，确保模型读到正文。
- 改动前失败、修改前影响分析、纯向量探针见[召回专项](../../../docs/internal/rebuild/evidence/knowledge-recall-20260918.md)；复合修复经历5项初始失败与2项独立复核边界失败，修复后保留对应回归。

## 检索配对结果（已完成）

对原40条已知检索问题，分别使用HEAD的KnowledgeBase/MCPToolManager和修改后的两模块。每组建立独立真实Chroma集合，载入相同24篇知识，固定deepseek-flash、Top-3、温度0及无SDK重试；逐题交替组别先后顺序。标签不提供给召回或重排。

| 指标 | 修改前完整检索链 | 修改后完整检索链 |
| --- | ---: | ---: |
| 文档级宏平均Recall@3 | 51.25% | 97.5% |
| Hit@3 | 55% | 100% |
| 必需文档全部命中率 | 47.5% | 95% |

Recall@3增加46.25个百分点。本轮旧链实测51.25%，与上轮50%存在模型波动，未挑选上轮或本轮的较优值。词法召回与重排修复是组合变更，不能将增益单独归因某一项。

另采集30道新回答题的两组召回快照。总计70题/140组检索，无检索调用失败；旧组aq01-hospital和新组retrieval-17各发生一次无效重排索引，按原有降级返回并保留。两个临时集合均已删除。完整输入、输出、源码指纹、旧源码快照与降级在[retrieval](retrieval)目录；旧40题不是未知留出集。

## 业务与回答评测

业务最终批次完整执行80任务/145轮，deepseek-flash真实调用，0重试；使用修复前相同固定输入，结果见[business-final/summary.json](business-final/summary.json)：

| 指标 | 修复前 | 修复后 |
| --- | ---: | ---: |
| 复合首轮号源＋材料双交付 | 11/20（55%） | 20/20（100%） |
| 上下文衔接 | 19/20（95%） | 20/20（100%） |
| 预约创建和取消完成 | 40/40（100%） | 40/40（100%） |
| 业务链回答质量轮次通过 | 140/145 | 145/145 |

修复后20条复合任务均有双角色与实际号源/材料卡片，不以仅出现角色名作为完成。全部100份运行源指纹未变、部署源码未变、接受基线未变；模型配置和前后指纹均保存在business-final。历史业务对照不是同时间随机试验，且20条复合表达属于已知回归集，100%仅表示本批20/20，不能外推所有用户问法。

回答双组共60份回答及60次Judge调用已全部执行，每组固定分母30：

| 指标 | 修改前检索证据 | 修改后检索证据 |
| --- | ---: | ---: |
| 回答综合通过 | 16/30（53.33%） | 28/30（93.33%） |
| 单文档题通过 | 13/24 | 23/24 |
| 跨文档题通过 | 0/2 | 1/2 |
| 资料不足题通过 | 3/4 | 4/4 |
| 有效Judge | 30/30 | 29/30 |

综合通过率增加40个百分点。这里的“通过”要求五维均至少0.8、全部目标事实覆盖且无无依据断言，不是单独的事实准确率。所有调用完成，但新版有1条无效Judge，因此原始report的complete与complete_pairs为false；不是仍有请求待执行。结果保留candidate，不补跑、不删除失败项或改变分母，原始证据见[answers/report.json](answers/report.json)。

两条新版未通过结果复核：

- `aq21-fever-rule`：Judge将原文中句号改为分号并截去末尾句号，quote不再是实际证据的逐字子串，被验证器拒绝。原始Judge虽给出全1分，仍计judge_invalid，不据人工阅读改为通过。这是评分有效性问题，不应直接解释为回答错误。
- `aq25-child-arrival-fee`：Top-3返回儿童材料、儿科介绍与费用，漏掉arrival-checkin。回答交付材料、费用和未付款边界，却未交付一层导诊台报到指引。20元/2000分均已正确回答，失败不是金额单位标签造成的。说明固定Top-3在多项跨文档问题上仍有覆盖缺口。

首轮业务长测在独立复核发现边界缺陷后停止，已完成24条响应保留在[business](business)，运行中途未改产品源码、接受基线未变。停止时可能在途的服务端请求不重用为最终结果；该批不作完整80任务指标。修复后以business-final新目录从头执行固定80任务。

回答专项为30题双组、同一回答prompt和模型的证据条件生成测试；不是/chat全链路或临床验证。目标事实只给Judge，不给回答模型。记录五维评分、目标事实覆盖、逐字来源核验、无依据断言、4题资料不足边界以及全部调用/评分失败；具体方法见[评测说明](../../answer-quality.md)。同模型Judge的判断仍须结合原文复核。

独立标签复核未发现原文不支持的事实，保留三项解释：aq25回答20元已满足费用需求，不应强求同时写2000分；aq29已给明天及医生，无需重复索取；资料不足题因遗漏某项后续建议而整体未通过，不等于拒答不当或存在幻觉。本轮不据结果修改标签或分母。

## 测试及已知限制

最终全量测试839通过、28跳过、1失败（139.26秒；初轮835通过、28跳过、同一失败）：`test_frozen_file_hashes_and_original_regression_files_are_unchanged`。失败来自semantic_intent manifest中两份默认回归题集的既有字节指纹不一致；本轮未改这些文件，当前文件与HEAD解析后的JSON一致，HEAD原始字节也不匹配历史manifest。不修改历史冻结证据来消除此失败。

最终复合边界定向回归175通过；真实HTTP Chroma初始化、来源、上传、重新连接和独立空集合检查1通过。知识词法目前逐请求扫描当前小库文本，不宣称大语料吞吐提升。U002原LLM故障门槛与接受基线状态不因本次修复自动改变。

[真实Redis边界检查](compound-boundaries.json)另外验证拒绝查号保留原选择、另一患者不受影响、本人首次与儿童材料类型正确，4项均通过，临时键清理为0。这四项不调用模型，不能混入模型回答成功率。

本地Docker API与前端已使用修复版重建，8088首页、8088/api/health、8000/health均200。测试XML见[checks](checks)，最终证据核对见[verification.json](verification.json)，评分失败归因见[answers-failure-review.json](answers-failure-review.json)。本轮未提交或推送；旧报告、简历既有修改和范围外文件保留。
