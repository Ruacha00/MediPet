# 带数据指标的简历模板

沿用原版“技术方案＋业务作用＋量化成果”的写法，提供强指标版与精简投递版。正文使用实测结果，完整数据口径和失败记录放在文末说明及[最新修复对照](../evaluation/reports/delivery-recall-20260918/notes.md)中；既有意图与并发事务指标保留原批次依据。

## 定位

### 稳妥版

```text
AI 应用工程 / 后端开发方向，具备多 Agent 编排、RAG 检索增强、会话记忆、动态规则注入与自动化评测闭环的项目实践。
```

### 进取版

```text
面向就诊与健康信息场景的 Agent 工程开发者，能够将 LLM 从单轮问答封装为可路由、可检索、可记忆、可观测、可评测的业务处理系统。
```

## 简历顶部摘要

```text
具备 Python/FastAPI 与 LLM 应用工程实践，围绕 MediPet 构建多 Agent 就诊服务链路，
涵盖多源意图识别、RAG、分层记忆、动态 Skills、预约事务和自动化评测。
自建评测中意图识别准确率 99.46%，知识检索Recall@3达到 97.5%，预约任务完成率 100%。
```

## 项目经历

```text
MediPet 多 Agent 就诊服务与健康咨询系统
技术栈：Python / FastAPI / DeepSeek / BGE / Redis / ChromaDB / Vue 3 / Docker Compose / Prometheus
项目背景：面向医院、诊所及健康服务平台的智能就诊服务系统，支持就诊咨询、科室分流、号源查询、预约办理、用药信息查询与检查报告预处理，结合领域知识库、分层记忆、动态 Skills 和运行监控，实现可解释、可观测、可评测的多 Agent 服务链路。
```

### 强指标版

```text
- 构建多源证据融合的意图识别与路由链路，结合 LLM 上下文理解、BGE 中文语义匹配和关键词规则，
  输出意图、结构化实体及4级紧急度，驱动6类 Agent 主辅协作；
  在186条自建意图评测集上，识别准确率达到 99.46%，Macro-F1 达到 0.995。

- 实现结构化主辅 Agent 并行编排，将号源查询与就诊准备拆分为独立只读任务，
  通过角色工具约束、材料类型校验和业务卡片交付，解决复合咨询中漏查、漏答及材料错配问题；
  在自建复合任务评测中，首轮双任务交付完成率由 55% 提升至 100%。

- 构建按需调用的混合 RAG 链路，结合 ChromaDB 向量检索、中文 BM25、加权 RRF 融合与 LLM 重排，
  配合查询改写、并行召回及缓存熔断，提升就诊流程与健康资料查询质量；
  在40条自建检索问题上，Recall@3由 51.25% 提升至 97.5%；
  在30条自建问答对照中，基于检索证据的回答综合通过率由 53.33% 提升至 93.33%。

- 扩展 Redis + ChromaDB 分层记忆，结合会话压缩、历史摘要和用户画像组织上下文，
  以患者和就诊事项隔离记录，支持连续追问、日期时段修改、选号改选及会话恢复；
  在20个自建多轮任务中，上下文状态衔接成功率达到 100%。

- 实现动态 Skills 热加载，将就诊准备、预约办理、症状分流和药品咨询等业务规则从代码中解耦，
  按角色和用户关键词注入系统提示；支持6组场景规则运行时更新，以5000字符注入预算控制上下文开销。

- 打通号源查询、方案生成、用户确认和预约取消流程，基于 Redis 乐观事务与幂等回执处理重复确认和库存竞争，
  配合 Vue 业务卡片完成就诊操作闭环；在40个自建预约任务中，创建与取消完成率达到 100%，
  10并发末号竞争测试中未出现超卖。

- 搭建 Monitor + LLM-as-Judge 评测闭环，采集 Agent/工具成功率、延迟与路由评分，
  从相关性、准确性、完整性和有用性四个维度评价回复，结合业务状态校验覆盖80个任务、145轮对话，
  补充检索证据支持、目标事实覆盖与资料不足处理的回答专项，支持失败定位和回归对比。
```

### 精简投递版

```text
- 实现多源证据融合的意图识别与六角色主辅编排，自建意图集准确率 99.46%、Macro-F1 0.995。
- 实现主辅 Agent 并行协作与工具交付约束，自建复合任务首轮完成率由 55% 提升至 100%。
- 构建中文 BM25 与向量融合的 RAG 链路，自建检索集Recall@3达到 97.5%，提升 46.25 个百分点；
  检索证据问答综合通过率达到 93.33%。
- 结合分层记忆、动态 Skills 和 Redis 幂等事务打通预约流程，自建预约任务完成率 100%，
  支持多轮事项恢复、可追踪的工具调用与自动化评测。
```

指标来源：[最新修复对照](../evaluation/reports/delivery-recall-20260918/notes.md)与[意图扩测](../evaluation/reports/business-expanded-20260918/notes.md)。可按岗位选取3—4条作为项目经历。

## 面试介绍模板

```text
MediPet 是面向就诊服务的多 Agent 系统，核心是把咨询、查号、准备预约和用户确认串成完整流程。

请求进入后，先识别用户意图与实体，再结合患者事项上下文分配主辅 Agent。
静态就诊资料通过 RAG 查询，动态号源和预约直接调用业务服务，处理结果以文字和业务卡片呈现。

我重点介绍多源意图识别、检索优化和预约闭环：自建意图集准确率 99.46%，
检索Recall@3由51.25%提升至97.5%，复合咨询首轮交付由55%提升至100%。
预约确认使用 Redis 事务和幂等回执，连续追问通过分层记忆与独立事项历史衔接。
```

面试展开时结合本人负责的模块，说明实现选择、遇到的问题和验证依据。项目使用预置业务数据；医院系统接入、个人职责和效果指标范围见文末资料，不将项目演示描述为生产客户业绩。

## 指标怎么算

### 意图路由链路

- `intent`：取三路融合后的一个主要分类结果；当前枚举含业务类别和通用兜底，不把“66条样本”误写成66类意图。
- `intent_group`：将具体类别归入query、guidance、appointment、escalation等粗组。三个医疗类别的group仍为query，编排根据具体intent选Triage/Medication；复合需求另选辅助角色。
- `source_scores`：LLM、当前向量后端、Pattern的输入分数，用来解释判定，不是各分支准确率或校准概率；实际是semantic还是hash另看 `embedding_info`。
- `4 级紧急度`：LOW / MEDIUM / HIGH / CRITICAL是规则输出；若单独衡量它，需要独立标签集，当前66条意图结果不是紧急度准确率。
- `结构化实体`：可以按字段exact match或field-level precision/recall/F1测量；这是测量方案，不是当前已经得到的指标。

### 常用公式

- `Accuracy = 正确预测数 / 总样本数`
- `Precision = TP / (TP + FP)`
- `Recall = TP / (TP + FN)`
- `F1 = 2PR / (P + R)`
- `Macro-F1 = 各类别 F1 的平均值`
- `故障接受准确率 = 接受且正确数 / 接受数`；全拒绝时没有可计算的接受准确率，不能记成100%。
- `故障覆盖率 = 接受数 / 可判定子集总数`；必须和接受准确率一起看。

### LLM-as-Judge 能做什么

- 可以：辅助标注模糊样本、判断回复质量、校验实体归一化、给路由解释打分、生成回归分析建议。
- 不适合单独承担：最终的 Accuracy、Precision、Recall、F1 统计。
- 原因：这些分类指标需要稳定的 gold label 和可复现的预测输出，最终应由评测脚本基于标注集计算；LLM-as-Judge 更适合做标注辅助和主观质量评分。



## 逐条说明


1. `三路识别 + 4级紧急度 + 6角色`：三路提供主要意图，编排选择主辅角色；不是三个大模型投票或每次六角色并行。
2. `186条意图评测`：由既有自建意图样本构成，185条预测正确；66条是旧默认回归规模。不是约500条人工标注或线上用户样本。
3. `Accuracy / Macro-F1 / 单类指标`：预测与标签由程序比较；固定66条单次全对不代表任意输入都正确。下方U001旧报告不证明U002语义效果；新分支须区分原始排名、同份LLM融合回放及故障接受准确率/覆盖率。
4. `6类角色`：General、Guidance、Appointment、Escalation、Triage、Medication；默认每类1实例，不是已部署集群。
5. `24份默认知识`：17份医院资料与7份医疗资料；文档数量不代表医学覆盖率。
6. `约500字符切片 + 最多3个改写查询`：按句号/换行组织切片，长句不一定严格500；原问题也保留，随后并行召回、去重、重排。
7. `回答综合通过率93.33%`：30题中28题通过五维评分与事实覆盖检查，1条Judge无效、1条跨文档信息缺失仍计入分母；是检索证据条件下的生成专项，不称端到端回答准确率或临床准确率。
8. `Recall@3为97.5%`：40题按相关文档集合计算宏平均召回率，同批旧链51.25%，提升46.25个百分点；不是命中率，也不与上一轮直接向量26.25%的口径拼接。
9. `Redis 24h TTL + 15条压缩 + 最近5条`：约束工作窗口，过期不是全部历史删除；完整事项历史另存。
10. `6组Skills热加载`：文件修改后主动reload才影响后续普通角色prompt，不是自动监听或运行时授权新工具。
11. `5000字符注入预算`：控制总体Skill文本，单份正文默认3200；字符预算不是token预算，附加标题/截断标记也需区分。
12. `15组多轮 + 12组边界`：既包含模型对话，也包含明确界面动作和事务条件；不能全部等同单次自然语言问答。
13. `四维Judge与五维专项`：端到端四维Judge保持原口径；新增五维回答专项另外报告固定分母通过率、全分母均值和有效评分均值，评分失败单列，不能混用。
14. `0.75阈值`：四维平均的质量通过线；模型质量高分不能覆盖业务状态失败。
15. `5%回归检测`：与人工已接受基线比较相对下降，先核对模型/数据/版本，不是每轮自动接受或自动修复。
16. `300s缓存 + 30s超时 + 熔断`：用于实际经过管理器的静态知识工具；连续5次失败/60秒恢复，不套到动态患者预约。
17. `/monitor 和 /metrics`：运行观测出口；请求成功率不等于回答正确率、临床准确率或生产SLA。
18. `精简投递版`：挑选本人讲得清的职责和两三项机制，详细指标与证据留作品集。
19. `HR开场白`：先说服务对象、业务闭环和本人贡献；如需数字，只用与职责直接相关且已核对的一项。

## 指标口径补充

### 当前正文采用的实测指标

| 指标 | 正文数值 | 测量依据 |
| --- | --- | --- |
| 意图识别准确率 / Macro-F1 | 99.46% / 0.995 | 前一轮对186条已有自建样本重新推理，185条正确；本次未修改识别器或重测该项 |
| 复合任务首轮交付完成率 | 55%→100% | 同一20条回归输入，号源与材料首轮双卡片由11/20提升至20/20 |
| 预约创建与取消完成率 | 100% | 修复后40个任务全部完成，与修复前一致；覆盖3科室、2患者身份、6个日期与首项/次项选择 |
| 多轮上下文状态衔接成功率 | 100% | 修复后20个日期时段修改、选号、窗口恢复与改选任务全部完成，原19/20 |
| Top-3相关文档召回率 | 51.25%→97.5% | 40条问题、新旧完整检索链配对；按目标文档集合计算宏平均，增加46.25个百分点 |
| 检索证据问答综合通过率 | 53.33%→93.33% | 同一30题分别使用新旧召回证据、同模型和评分规则，16/30→28/30，增加40个百分点 |

最新证据：[修复与回答对照](../evaluation/reports/delivery-recall-20260918/notes.md)。意图指标沿用[前一轮扩测](../evaluation/reports/business-expanded-20260918/notes.md)，Redis并发依据[首轮专项](../evaluation/reports/business-20260918/notes.md)。这些是自建评测结果，预约显式确认由测试模拟用户动作；20条复合任务属于已知回归集，不能外推任意输入100%成功。

回答专项使用deepseek-flash生成和评分，覆盖24条单文档题、2条跨文档题、4条资料不足题。每个维度至少0.8、所有目标事实覆盖且无无依据断言才通过；新版28条通过，30条分母中保留1条无效Judge和1条漏召回造成的未通过。全部60份回答、60次Judge调用保留，未补跑或改分；不写成“人工判定准确率93.33%”。

本轮是组合改进：词法与向量融合补候选，重排正文截断修复让模型读到有效内容。不能把46.25个百分点全部归因于BM25，也不能将结果写成BGE知识库升级效果；BGE用于意图分支，知识库仍使用现有集合的embedding。

修复前历史结果继续保留：复合交付11/20、上下文19/20、业务链质量140/145；上一轮直接向量26.25%→改写召回重排50%的增益为23.75个百分点。本轮同批旧链重测51.25%，采用同批51.25%→97.5%对照，不择优拼接。

如果面试官追问数据来源，可以按下面口径讲。先保留2026-09-16的U001医疗扩展历史，再看2026-09-17的U002语义专项；不能混用旧版54条结果或把不同批次相加。

### 2026-09-18首轮小样本专项（历史记录）

| 简历位置 | 可核实结果 | 使用方式 |
| --- | --- | --- |
| 意图识别 | 66/66固定回归，Macro-F1 1.0 | 标明固定回归集，不写成泛化准确率100% |
| 预约闭环 | 创建5/5、取消5/5 | 可以合称10组任务完成；真实模型对话后由夹具模拟明确确认动作 |
| 分层记忆 | 7/7上下文与事项状态检查 | 明确测了日期/时段、序号、患者隔离、工作窗口失效、改选和归档 |
| 事务一致性 | 50/50重复创建/取消组，30/30末号竞争组 | 每组10并发；说明真实Redis，非模型能力、非生产压测 |
| 多Agent协同 | 3/5首轮双角色、双卡片交付 | 暂不包装成效果亮点；1条仅文字覆盖、缺材料卡片，1条错误澄清 |
| RAG | 4/5复合任务首轮成功检索且带来源 | 来源存在不等于相关性或召回率，不写RAG准确率80% |
| 评测体系 | 37/39业务状态组、52/54质量轮次 | 两个分母分别讲；失败作为评测发现问题的案例 |

专项依据当前main部署，使用deepseek-flash、中文BGE、真实Redis/Chroma。新增表达由既有业务模板设计，不称独立盲测集；一次执行，不择优重跑。创建/取消各5组规模有限，直接写分子/分母，避免笼统宣传成功率100%。

事务共1,300次并发请求，包含明确冲突拒绝：134次成功、1,084次`conflict`、82次`target_unavailable`；另100次顺序重放获得原回执。80组最终数据一致并不等于1,300次全部直接成功。完整条件、原始结果及失败见[专项报告](../evaluation/reports/business-20260918/notes.md)；人工分流降低率、挂号转化率、节省工时仍无数据，不补猜测值。

解释首轮历史问题时可以说：“我先把任务完成拆成对话、确认动作和真实状态检查。在当前专项中，10组预约创建/取消闭环完成；另用真实Redis测了50组重复操作和30组末号竞争。与此同时，复合咨询只有3/5满足双角色、双卡片标准，所以我没有用整体通过率掩盖这项不足。”

本次都是候选证据，不自动接受为基线，也不关闭U002既有故障准确率门槛问题。

### U001历史记录

| 层次 | 已记录事实 | 必须同时说明的范围 | 证据 |
| --- | --- | --- | --- |
| 后端 | 684 passed、21 skipped | 跳过不计通过；全量早于最后Guidance提示修复 | [JUnit](../docs/internal/updates/U001-health-consultation/evidence/backend-tests.xml) |
| 前端 | 35项与构建通过 | 组件检查不是模型质量率 | [交付记录](../docs/internal/updates/U001-health-consultation/evidence/reports.md) |
| 最后提示修复 | 119项定向回归通过 | 未重跑全部后端；不与684相加 | [验收](../docs/internal/updates/U001-health-consultation/evidence/acceptance.md) |
| 完整模型输入 | 66意图、15组多轮、12组边界 | deepseek-flash，thinking disabled，单次固定输入 | [完整报告](../evaluation/reports/live-20260916-u001/notes.md) |
| 完整模型结果 | 66/66意图、30/30质量、26/27业务，57/58结果项 | 58=1意图汇总+30质量+27业务，不是93输入的统一准确率；`candidate`未接受 | 同上 |
| 局部复测 | 原样两轮3/3 | 2质量+1业务；未重跑完整58项 | [路线复测](../evaluation/reports/live-20260916-u001-route-fix/notes.md) |

真实存储、OCR与浏览器另有[验收记录](../docs/internal/updates/U001-health-consultation/evidence/acceptance.md)，不与单元测试混成一个准确率。完整评测HTTP耗时188.562秒、局部两轮7.125秒是不同输入量的批次时间，不是P95或性能提升对照。

### U002语义专项：可以使用的数字与限制

下表来自[正式冻结对照](../docs/internal/updates/U002-semantic-intent/evidence/calibration/implementation.md)。真实ONNX排名与真实DeepSeek分类响应已采集，三模式重放共享同一份响应；不是三个模式分别调用LLM后挑更好的一次。故障行则是明确注入LLM失败的对照，不是线上故障频率。

| 指标 | semantic | hash | disabled |
| --- | --- | --- | --- |
| 单句向量单路Top-1 | 95/160（59.375%） | 62/160（38.75%） | 不适用 |
| 单路Macro-F1 | 0.456180 | 0.287303 | 不适用 |
| 低字面重合单路Top-1 | 80/132（60.606%） | 52/132（39.394%） | 不适用 |
| 同一LLM响应的留出融合 | 185/186（99.462%） | 185/186（99.462%） | 185/186（99.462%） |
| 原66条融合回归 | 66/66 | 66/66 | 65/66 |
| 故障时已接受结果准确率 | 69/84（82.143%） | 18/18（100%） | 6/6（100%） |
| 故障时覆盖率 | 84/160（52.5%） | 18/160（11.25%） | 6/160（3.75%） |

可直接讲：“在冻结的低字面重合子集上，语义向量单路Top-1比字符hash高21.212个百分点；同一LLM响应下三模式融合持平。故障接管覆盖率达到预定线，但接受准确率未达90%，所以保留失败候选，未宣称U002验收完成。”这一整组边界应一起保留，不能只摘“提升21.212”或“融合99.462%”。它不是整套助手回答正确率，更不是临床准确率。

132条是低重合子集，160条是单句主集合，186条包含其他独立控制组。开发集只用于选门槛，不作为最终效果；已使用的留出集也不能再次充当未知数据。历史66全对和本轮语义/hash的66全对是两次不同的回归记录，不相加。

`candidate` 表示尚未被人工接受为基线，不自动等于测试失败；本轮未通过来自明确的故障准确率门槛。接受基线须记录复核者 `reviewed_by`，当前实现要求非空标识而非登录鉴权，也不替代HTTP或浏览器验收。`accept_baseline()`只适用于端到端评测的 `EvalReport`；U002语义专项JSON没有自动转换或接受入口。工程测试、有效证据、指标达标与人工接受分别陈述。

当前扩测已补充直接向量检索与改写/召回/重排的Recall@3对照。并行提速、成本下降、OCR字段准确率、最大并发容量及生产吞吐仍未测量，后续以独立专项补充。

## 面试追问版

1. 细粒度识别与路由：项目使用LLM、本地中文语义模板向量与Pattern融合，字符hash为明确后备，输出主要意图与实体；领域打分和协作规则再选主辅角色。我会用“查号+材料”说明两步差别，而不称任意任务自动生成DAG。
2. 意图数据：gold label是固定用例期望类别，预测由识别器产生，Accuracy和F1由脚本计算。历史66条回归继续保留；U002另按表达家族分开开发/留出，标注与低字面重合规则先冻结且独立复核，再推理。开发集仅选门槛，留出不能反向改模板或阈值；共享真实LLM原响应用于三模式回放，假LLM只证明机制。数字核对正式候选，个人标注/实现职责按实际填写。
3. 按需RAG：路由后角色选择是否检索；改写、并行召回、片段去重与重排保留，动态预约直接查询。共享管理器在进程内，不声称远程MCP服务。当前加入中文BM25与加权RRF，并修复重排正文截断，召回收益按40题完整链配对报告说明。
4. 分层记忆：工作窗口控制TTL和长度，Chroma保存摘要及资料，完整历史在Redis另存；身份作用域是参与者/患者/事项。异步Redis与线程池用于避免同步调用阻塞，关键状态仍回查服务。
5. 动态Skills：规则文件按角色/关键词注入并主动reload，角色权限仍由程序控制。分诊、药品和报告的有界信息不是诊断，也不能靠prompt自动获得更多权限。
6. Monitor与Judge：前者观察运行并影响同类实例分数；后者评价回答，业务断言检查真实状态。一个答案可能Judge通过但没有完成工具查询，路线失败与定点复测就是现有例子。
7. 说明gold label：就是“期望标签”。例如输入“明天儿科还有号吗”，期望为slot_query；实际预测是否相同由程序核对。它不是Judge分数，也不是临床标准答案。
8. 样例与药品边界：分类模板和编码参考句不会更新BGE权重，也不是药品白名单。换药名仍按查询目的匹配意图；药品工具只允许来自用户原话的药名/剂型，未知药物不能替换成样例中的已知药品。分类正确也不等于资料已收录。

职责表达：我实际负责[模块]，针对[可复现问题]完成[修改]，用[已有证据]验证[范围]；其他机制属于沿用或阅读，不据此声称全部原创。
