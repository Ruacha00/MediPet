# 带数据指标的简历模板

指标分成两类：

1. 项目内可直接验证的结构与配置，比如6类Agent、6组Skills、Redis工作窗口24h TTL、15条压缩阈值、最近5条保留、LLM-as-Judge四维评分和相对已接受基线5%的回归检测；
2. 需要真实数据支持的效果指标，比如固定样本准确率、Macro-F1、Top-K命中率、P95延迟和成本。已有测量与待测指标必须分开，结构参数不代表效果提升。

本文保留不同长度模板与逐条释义。原材料中未核验的500条样本、91.3%、RAG88.7%等数字不沿用；也不填无证据的95%、提速30%、节省50%或千会话。个人职责按[简历包装](简历包装.md)填写，不默认“独立实现”全部模块。

## 定位

### 稳妥版

```text
AI 应用工程 / 后端开发方向，具备多 Agent 编排、RAG 检索增强、会话记忆、动态规则注入与自动化评测闭环的项目实践。
```

### 进取版

```text
面向就诊与健康信息场景的 Agent 工程开发者，能够将 LLM 从单轮问答封装为可路由、可检索、可记忆、可观测、可评测的业务处理系统。
```

## 简历顶部摘要

```text
具备Python/FastAPI与LLM应用工程实践，围绕MediPet完成[本人实际负责的扩展、联调或验证]。
项目采用三路意图、六角色主辅路由、工具白名单、按需RAG、Redis + Chroma记忆与动态Skills，
支持患者事项、幂等预约确认、本地报告OCR和有来源健康信息，并通过业务断言与四维Judge分层验证。
```

方括号填写后再投递。顶部摘要不必堆测试总数；能解释的职责与一条业务闭环比十个孤立参数更有用。

## 项目经历

```text
MediPet 多 Agent 就诊与健康信息助手
技术栈：Python / FastAPI / DeepSeek兼容API / Anthropic SDK / Redis / Chroma / Vue / Tesseract / Docker
项目背景：面向明和虚构医院，支持本人和家属的资料、号源、预约、材料与文字指引，
以及有限症状方向、两个指定美国药品标签和原文报告预处理；不接真实医院、不诊断或开处方。
```

### 强指标版

这里的“强指标”指可追溯参数和实测结果，不把尚未验证的收益写成成绩。以下要点按个人实际职责选取。

```text
- 适配LLM Few-shot、字符n-gram模板向量和关键词Pattern三路识别，正常权重0.7/0.2/0.1，
  输出意图、意图组、来源分数、4级紧急度和实体，驱动六角色主辅分工；
  在固定66条意图输入的单次deepseek-flash评测中，Accuracy与Macro-F1均为1.0，限于该输入集。

- 构建角色按需调用的RAG链路，使用Chroma检索、原问题加最多3个改写查询并行召回、
  稳定片段ID去重与LLM重排；静态知识工具配置300s缓存、默认30s超时、熔断与fallback。
  当前24份默认知识，不以此推导召回率；Recall@k、消融对照和成本变化尚未测量。

- 扩展Redis + Chroma分层记忆，工作窗口24h TTL，达到15条时压缩并保留最近5条；
  新增独立完整事项历史、患者作用域与窗口过期恢复，动态业务不依赖摘要判断。
  使用异步Redis与线程池处理同步Chroma访问，未声称经压测证明吞吐提升。

- 配置6组Skills及显式热加载，按角色和关键词注入规则；
  单份正文默认3200字符、总预算5000字符，关键边界同时由工具白名单与业务校验执行。
  Escalation直接执行固定代码，不通过热加载改变急症固定响应。

- 接通方案准备、按钮确认与取消，Redis WATCH/MULTI/EXEC校验库存和状态，
  原回执幂等重放及稳定消息ID支持重复请求和历史写入失败后的补齐。
  报告支持本地文字/PDF/图片提取，10MB、PDF最多10页，原文范围比较而非诊断。

- 保留监控与四维LLM Judge，并增加患者、工具、卡片和库存等业务断言。
  完整模型候选的30/30质量、26/27业务加意图汇总共57/58结果项；
  路线问题修复后仅原样两轮3/3通过，候选未接受，不改写为完整58/58。
```

### 精简投递版

```text
- 适配三路意图和六角色主辅协作，输出可观察路由与工具结果，支持查号与材料等复合需求。
- 接通24份知识、6组Skills与患者事项记忆，将静态检索、动态预约和当前报告事实分开。
- 用Redis乐观事务、显式确认与幂等回执完成预约闭环，并以业务断言、真实组件和模型候选分层验证。
```

如果本人负责评测，可另加一条：“固定66条意图样本全部命中；完整业务与质量结果、失败和局部复测另存可核对报告。”不必把所有验收计数塞进同一段。

## 面试介绍模板

```text
MediPet可以理解成一个面向门诊就诊与健康信息的Multi-Agent Runtime。

用户请求进入系统后先校验患者事项，并检查有界急症规则。普通路径读取当前上下文，
完成细粒度意图和实体识别；随后编排器根据意图、关键词与实体确定主辅角色。
比如“查孩子明天儿科号源，再说需要带什么”，Appointment和Guidance可以并行处理。

角色可按需查Chroma资料，结合Redis工作窗口、患者范围内摘要与Skills生成上下文。
动态号源和预约直接查服务，报告追问只读当前事项最新卡片。选择号源只产生方案，
用户点击确认后才执行Redis事务，重复请求取原回执。

我实际负责[范围]，重点解决[具体问题]。项目保留三路识别、检索、记忆、规则和评测主线，
新增患者事项、预约事务与有界医疗信息。能确认的是机制和固定样本结果；
若讨论召回、延迟或成本提升，我会先给同条件对照证据，当前没有的数据就不写。
```

此段保留从“请求→上下文→路由→工具→验证”讲解的顺序；个人职责不得用项目能力代填。

## 指标怎么算

### 意图路由链路

- `intent`：取三路融合后的一个主要分类结果；当前枚举含业务类别和通用兜底，不把“66条样本”误写成66类意图。
- `intent_group`：将具体类别归入query、guidance、appointment、escalation等粗组。三个医疗类别的group仍为query，编排根据具体intent选Triage/Medication；复合需求另选辅助角色。
- `source_scores`：LLM、字符模板相似度、Pattern的输入分数，用来解释判定，不是各分支准确率或校准概率。
- `4 级紧急度`：LOW / MEDIUM / HIGH / CRITICAL是规则输出；若单独衡量它，需要独立标签集，当前66条意图结果不是紧急度准确率。
- `结构化实体`：可以按字段exact match或field-level precision/recall/F1测量；这是测量方案，不是当前已经得到的指标。

### 常用公式

- `Accuracy = 正确预测数 / 总样本数`
- `Precision = TP / (TP + FP)`
- `Recall = TP / (TP + FN)`
- `F1 = 2PR / (P + R)`
- `Macro-F1 = 各类别 F1 的平均值`

### LLM-as-Judge 能做什么

- 可以：辅助标注模糊样本、判断回复质量、校验实体归一化、给路由解释打分、生成回归分析建议。
- 不适合单独承担：最终的 Accuracy、Precision、Recall、F1 统计。
- 原因：这些分类指标需要稳定的 gold label 和可复现的预测输出，最终应由评测脚本基于标注集计算；LLM-as-Judge 更适合做标注辅助和主观质量评分。



## 逐条说明


1. `三路识别 + 4级紧急度 + 6角色`：三路提供主要意图，编排选择主辅角色；不是三个大模型投票或每次六角色并行。
2. `66条意图评测`：文件中有固定输入与gold label，不是约500条人工标注或线上用户样本；不能自行补造标注过程。
3. `Accuracy / Macro-F1 / 单类指标`：预测与标签由程序比较；固定66条单次全对不代表任意输入都正确。
4. `6类角色`：General、Guidance、Appointment、Escalation、Triage、Medication；默认每类1实例，不是已部署集群。
5. `24份默认知识`：17份医院资料与7份医疗资料；文档数量不代表医学覆盖率。
6. `约500字符切片 + 最多3个改写查询`：按句号/换行组织切片，长句不一定严格500；原问题也保留，随后并行召回、去重、重排。
7. `RAG回答准确率`：需要问答标准与人工/规则核验；当前没有可沿用的65%→83%或88.7%结果。
8. `检索召回率变化`：需要相关文档gold set与同条件对照；目前不填91.2%或提升若干百分点。
9. `Redis 24h TTL + 15条压缩 + 最近5条`：约束工作窗口，过期不是全部历史删除；完整事项历史另存。
10. `6组Skills热加载`：文件修改后主动reload才影响后续普通角色prompt，不是自动监听或运行时授权新工具。
11. `5000字符注入预算`：控制总体Skill文本，单份正文默认3200；字符预算不是token预算，附加标题/截断标记也需区分。
12. `15组多轮 + 12组边界`：既包含模型对话，也包含明确界面动作和事务条件；不能全部等同单次自然语言问答。
13. `四维Judge`：相关性、准确性、完整性、有用性；评分失败单列，不用假零分污染有效均值。
14. `0.75阈值`：四维平均的质量通过线；模型质量高分不能覆盖业务状态失败。
15. `5%回归检测`：与人工已接受基线比较相对下降，先核对模型/数据/版本，不是每轮自动接受或自动修复。
16. `300s缓存 + 30s超时 + 熔断`：用于实际经过管理器的静态知识工具；连续5次失败/60秒恢复，不套到动态患者预约。
17. `/monitor 和 /metrics`：运行观测出口；请求成功率不等于回答正确率、临床准确率或生产SLA。
18. `精简投递版`：挑选本人讲得清的职责和两三项机制，详细指标与证据留作品集。
19. `HR开场白`：先说服务对象、业务闭环和本人贡献；如需数字，只用与职责直接相关且已核对的一项。

## 指标口径补充

如果面试官追问数据来源，可以按下面口径讲；所有记录均对应2026-09-16医疗扩展，不能混用旧版54条结果。

| 层次 | 已记录事实 | 必须同时说明的范围 | 证据 |
| --- | --- | --- | --- |
| 后端 | 684 passed、21 skipped | 跳过不计通过；全量早于最后Guidance提示修复 | [JUnit](../docs/internal/updates/U001-health-consultation/evidence/backend-tests.xml) |
| 前端 | 35项与构建通过 | 组件检查不是模型质量率 | [交付记录](../docs/internal/updates/U001-health-consultation/evidence/reports.md) |
| 最后提示修复 | 119项定向回归通过 | 未重跑全部后端；不与684相加 | [验收](../docs/internal/updates/U001-health-consultation/evidence/acceptance.md) |
| 完整模型输入 | 66意图、15组多轮、12组边界 | deepseek-flash，thinking disabled，单次固定输入 | [完整报告](../evaluation/reports/live-20260916-u001/notes.md) |
| 完整模型结果 | 66/66意图、30/30质量、26/27业务，57/58结果项 | 58=1意图汇总+30质量+27业务，不是93输入的统一准确率；candidate未接受 | 同上 |
| 局部复测 | 原样两轮3/3 | 2质量+1业务；未重跑完整58项 | [路线复测](../evaluation/reports/live-20260916-u001-route-fix/notes.md) |

真实存储、OCR与浏览器另有[验收记录](../docs/internal/updates/U001-health-consultation/evidence/acceptance.md)，不与单元测试混成一个准确率。完整评测HTTP耗时188.562秒、局部两轮7.125秒是不同输入量的批次时间，不是P95或性能提升对照。

没有受控实验支持RAG准确率/召回率提升、并行提速、成本下降、OCR字段准确率或并发规模。以后测量须保存数据集、相关文档标签、模型参数、硬件、冷暖缓存、重复次数和失败原文。先保留变量，得到报告后再填数字。

## 面试追问版

1. 细粒度识别与路由：项目使用LLM、字符模板向量与Pattern融合，输出主要意图与实体；领域打分和协作规则再选主辅角色。我会用“查号+材料”说明两步差别，而不称任意任务自动生成DAG。
2. 意图数据：gold label是固定用例期望类别，预测由完整识别器产生，Accuracy和F1由脚本计算。当前能举证66条，不宣称自己人工标注500条；样本独立性、分布和重复运行还需单独研究。
3. 按需RAG：路由后角色选择是否检索；改写、并行召回、片段去重与重排保留，动态预约直接查询。共享管理器在进程内，不声称远程MCP服务或已实测召回收益。
4. 分层记忆：工作窗口控制TTL和长度，Chroma保存摘要及资料，完整历史在Redis另存；身份作用域是参与者/患者/事项。异步Redis与线程池用于避免同步调用阻塞，关键状态仍回查服务。
5. 动态Skills：规则文件按角色/关键词注入并主动reload，角色权限仍由程序控制。分诊、药品和报告的有界信息不是诊断，也不能靠prompt自动获得更多权限。
6. Monitor与Judge：前者观察运行并影响同类实例分数；后者评价回答，业务断言检查真实状态。一个答案可能Judge通过但没有完成工具查询，路线失败与定点复测就是现有例子。
7. 说明gold label：就是“期望标签”。例如输入“明天儿科还有号吗”，期望为slot_query；实际预测是否相同由程序核对。它不是Judge分数，也不是临床标准答案。

职责表达：我实际负责[模块]，针对[可复现问题]完成[修改]，用[已有证据]验证[范围]；其他机制属于沿用或阅读，不据此声称全部原创。
