# 使用服务器准备的权威操作确认快照

MediPet 的高影响写 Tool 只接收模型提供的最小业务参数，患者身份由已校验的就诊事项提供，面向参与者的确认资料则由服务器通过受信任依赖生成并随 Action Proposal 持久化；确认提交前必须用相同患者作用域重新校验该快照，变化、过期或资源不可用时拒绝旧提案。这个通用协议避免把模型生成内容当作预约确认事实，也避免让 Action Store 依赖具体医院系统，同时允许后续写 Tool 复用同一套准备、确认和重校验流程。
