# MediPet 实际模型评测与人工复核交接

日期：2026-09-16。模型 `deepseek-flash`，DeepSeek Anthropic 兼容入口，`MEDIPET_THINKING=disabled`。以下是两次实际页面触发的完整默认评测，均保存全部原始结果，**均未接受为基线**。

## 两次运行

| 候选 | 入口与版本 | 实际结果 | 状态 |
| --- | --- | --- | --- |
| [首份候选](../../../../evaluation/reports/live-20260916-full/notes.md) `0f457b4d311941aab9ad63a16dad216d` | 开发页面 5173 → API 8010；修正前 | HTTP 200 / 269.062 秒；39/49 结果项通过；9 项低 Judge 分、1 组协作业务失败 | 原始结果保留；有已证实的评测漏报和 Judge 背景不足，不能作为可靠最终基线 |
| [容器候选](../../../../evaluation/reports/live-20260916-container/notes.md) `a13acc91e787406bb909067a456be4a3` | 验证容器页面 18088 → API 18000；路由/评测修正后 | HTTP 200 / 170.344 秒；49/49 结果项通过；Judge/调用失败/跳过均 0 | candidate，等待用户人工复核 |

每次恰好点击一次“运行评测”，无请求 body，默认输入均为 54 条意图、12 组多轮、12 组边界；输入 SHA-256 均为 `9ea00961639b03fb3fd0b0a26c91df2537575d404c6bbb7bacc5cf3e3821d378`。两个运行内部的 62 个源码指纹分别前后一致，页面错误各为 0；没有自动重试或覆盖首份报告。

源码、Judge 背景与部署环境在两次之间发生变化，不能把分数或耗时差异描述成严格同条件优化提升。完整响应、时序、参数、输入、源码清单、截图和页面原文均随各候选保存。

## 容器候选的结果与实际范围

- 意图 54/54，Accuracy 1.0000、Macro-F1 1.0000。
- 有效质量项 24，全部达到 0.75 阈值；平均相关性 0.9917、准确性 1.0000、完整性 0.9583、有用性 0.9604。
- 业务组 24/24 满足断言，包括同轮角色与两类卡片、确认前不预约、创建/取消库存、重复确认、患者切换、历史/归档恢复、旧方案、过期、末号竞争、真实来源以及固定急症零模型调用。
- Judge 失败、调用异常、跳过和业务断言失败均为 0。49 个结果项由 1 个意图汇总、24 个质量项、24 个业务组组成，不等于把 78 个输入都计作一种准确率。

**保留限制**：容器运行中 `archive-restore` 首轮仍把“大厅”作为地点参数，`get_wayfinding` 返回 `not_found`。模型诚实说明未找到；归档恢复断言与 Judge 通过不能证明该简称可识别。另有预期的 `no_slots` 和注入 `retrieval_failed` 业务错误，完整 trace 未删除。输入识别采用本地字符 n-gram 向量，知识采用 Chroma `all-MiniLM-L6-v2`；这些分数不外推为开放场景准确率。

本套评测通过 HTTP 页面入口运行，但每个用例在隔离工厂内直接调用编排器与医院服务；每一步 HTTP 交互由 [I04](../specs/S06-api/issues/I04.md) 及[真实浏览器业务](browser.md)另证。实际浏览器记录包括公开医院查询、儿童号源与材料协作、按钮确认/取消、回执、普通事项管理和本人/家属切换，不能用 Judge 代替这些业务事实。

## 修正与配置证据

首份运行后保留原始候选，分别修正：

1. “需要的资料”等明确就诊资料措辞未触发 Guidance，采用窄关键词并保留纯准备预约/取消反例，见 [A02](../specs/S04-agents/issues/A02.md)。
2. `judge=False` 边界提前返回漏掉失败 trace；来源断言将结果数量误当来源数量；Judge 背景缺本轮实际卡片/轨迹/时钟。第二次保存 24 份 `judge_context`，评分算法与预算保持不变，细节见 [V02](../specs/S08-delivery/issues/V02.md)。
3. 首份同步请求实测 269.062 秒超过旧代理 180 秒，因此容器评测路由改为 1800 秒。第二次从容器完成 170.344 秒请求；该次没有实际跨越旧阈值，不写成已实测等待超过 180 秒。

运行前容器 agents/evaluator SHA、模型、thinking 和基线状态见[容器前置记录](../../../../evaluation/reports/live-20260916-container/container-before.json)；[容器运行后记录](../../../../evaluation/reports/live-20260916-container/container-after.json)确认接受基线仍不存在，候选与 HTTP 原文规范化 JSON SHA-256 相同（`7712e4f5b85e2c9b0472234f52bb4ea9c979359586df9fa1274aeee66600b07d`），run_id/status/accepted_by 一致。这是协调者实际核对容器卷的证据，与宿主脚本检查分开保存。

## 人工复核状态

两份报告 `accepted_by` 均为空，脚本未调用 `accept_baseline`。容器候选供用户查看完整回答、真实卡片/工具状态、评分背景及地点简称限制后，明确决定是否接受为首份基线。最终接受动作尚未执行，V06 保持进行中；全项通过不代替人工复核。
