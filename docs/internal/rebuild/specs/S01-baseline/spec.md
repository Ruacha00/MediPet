# S01 基准结构与运行环境

状态：B01—B04 已完成，对应总计划 P1 验收通过；P0 已在 `d0d2bdb` 完成。

## 来源与范围

依据[总计划](../../../../../IMPLEMENTATION_PLAN.md) §2、4、5、12、13 P1、14.2、15。为 R01—R12 提供运行基础；本 spec 不把业务能力标为已实现。

从固定后端和前端提交导入代码，统一 MediPet 命名、目录、依赖与开发/容器入口。保留既有识别、编排、工具、检索、记忆和评测算法，不复用已清理的旧 MediPet 技术实现。

## 行为与交付

- 后端沿用 FastAPI 及基准布局，前端沿用 Vue/Vite，只保留 Python 入口。
- Redis/Chroma 使用本项目数据空间和持久化配置；环境示例不含真实密钥。
- 开发代理和容器代理都支持 `/api/python`，运行配置只保留一套有效说明。
- 索引在代码导入后更新；命名修改与变更分析针对当前工作树。

## 原子 issue

| Issue | 交付结果 |
| --- | --- |
| [B01](issues/B01.md) | 可追溯的后端源码基准 |
| [B02](issues/B02.md) | Vue 前端及单一 Python 请求入口 |
| [B03](issues/B03.md) | 可启动的环境与容器配置 |
| [B04](issues/B04.md) | 原机制回归与当前工作树索引 |

## 依赖与并行

B01、B02 分别拥有后端和前端，可同时开始。B03 在两者交付后统一运行配置，B04 完成基准验收。其后的业务实现从 H01 共享契约展开，详见[依赖与调度](../../DEPENDENCIES.md)。

## 完成标准

- [x] 固定版本及必要差异可复核，输入目录与主工作区保持原状。
- [x] 假模型原机制回归、后端启动与健康检查、前端构建全部通过。
- [x] 开发与容器前端请求都能到达正确后端。
- [x] B01—B04 的记录完整，当前工作树索引对应实际导入代码。
