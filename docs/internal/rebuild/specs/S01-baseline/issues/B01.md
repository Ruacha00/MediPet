---
id: B01
spec: S01
title: 导入并命名后端基准
status: done
milestone: P1
depends_on: []
touches: ["api/**","agents/**","core/**","memory/**","mcp/**","monitor/**","evaluation/**","skills/**","tests/test_agent_orchestrator.py","requirements.txt","docs/internal/rebuild/source-map.md"]
---

# B01 导入并命名后端基准

## 目标

从固定后端提交建立可核对的 MediPet 后端基准，保留既有机制。

## 实施范围

- 复核总计划 §2.2 的后端提交与外部指纹清单，从该提交读取选定受版本控制文件；记录内部来源对照。
- 导入既有目录和应用依赖，按符号分析更新项目命名及相应引用；保留第三方许可证与适用声明。
- 保留四角色、三次模型工具循环、并行整合、记忆、RAG、监控与评测结构；业务内容后续由对应 issue 适配。

## 输入与输出

输入：固定后端提交 `3f5a163a9922799c28087f4d8519b6c692cd5d05`。输出：后端基准文件及 `docs/internal/rebuild/source-map.md` 中的后端来源/排除项/必要差异。该记录在本 issue 实施时创建。

## 验收

- [x] 导入文件可对应固定提交，源目录的未提交实验、密钥、依赖和运行数据没有进入工作树。
- [x] 后端模块导入检查与原机制测试的结果有记录；环境尚未就绪的运行检查交给 B03/B04，不记作通过。
- [x] 命名与引用一致；源码导入后为当前工作树建立索引，再对既有符号执行命名和影响分析。

## 依赖与并行

与 B02 并行；仅写后端范围和内部来源记录，前端来源记录由 B02 单独维护。共用运行配置、根 README 与环境参数交给 B03。

依赖以文件头的 `depends_on` 为准；共享路径是计划写入边界，开始前按当前源码复核。执行规则见[工作索引](../../../README.md)。

## 完成记录

负责人：`spec_agents_knowledge`（B01 后端范围）；已完成原样导入、逐处品牌/项目参数适配与本 issue 验收。

2026-09-16：从固定 Git 提交读取并导入 17 个跟踪文件，逐文件 SHA-256 与 Git blob 一致，其中 13 个与项目外固定指纹再次核对一致。源工作区状态前后相同，未改动输入目录；未导入环境文件、运行数据、旧部署实验或范围外运行配置。

导入文件、来源指纹、排除项、影响分析与适配差异见[后端来源记录](../../../source-map.md)。主代理建立本工作树索引后，显式绑定绝对路径进行上游分析，HIGH 加载日志/Composer 路径在修改前报告，UNKNOWN/动态调用结合源码核实；没有进行函数或类重命名。

9 个文件的品牌或 ECHOMIND 项目参数前缀已统一，17 个来源文件复核仅存在这些已审阅差异；SDK 的 ANTHROPIC 参数、依赖版本、默认值、角色和算法保持。B01 范围旧品牌扫描零匹配，场景转换交下游 issue。

验证：`.venv/Scripts/python.exe -m pytest -q tests/test_agent_orchestrator.py` 得到 **10 passed in 0.34s**；11 个后端模块导入成功；Skill 加载/重载、提示品牌及假客户端下的新 Composer/trace/角色模型参数覆盖检查通过。未运行真实存储、API 服务、实际模型或容器验收；未提交推送。

B01 文件写入已交还主代理。B03 可消费后端入口并接线运行配置；B04 继续当前索引修复、刷新及基准运行验收。当前来源记录不冒充提交前变更分析已通过。
