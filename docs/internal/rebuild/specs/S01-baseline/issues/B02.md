---
id: B02
spec: S01
title: 导入前端并收敛 Python 请求入口
status: done
milestone: P1
depends_on: []
touches: ["frontend/**","docs/internal/rebuild/frontend-source-map.md"]
---

# B02 导入前端并收敛 Python 请求入口

## 目标

建立可构建的 Vue/Vite 前端基准，并保留一个 Python 后端入口。

## 实施范围

- 从固定前端提交导入受版本控制文件到 frontend/，记录来源对照，统一 MediPet 名称。
- 保留既有对话、知识库、评测页面和请求封装，移除 Java 入口与自动回退。
- 沿用 `/api/python` 开发代理；锁文件与包清单保持一致。

## 输入与输出

输入：固定前端提交 `9fe65b392ef44c50d71c856294bb77c41f0e86a5`。输出：frontend/ 和内部前端来源记录，供 B03 接入容器。

## 验收

- [x] 包清单、锁文件和源码来自固定版本，未携带 node_modules、构建产物或本地配置。
- [x] 前端依赖安装与构建成功；在本 issue 内解决具体安装或构建错误并记录必要差异，再交给 B03 接入容器。
- [x] 静态检查与请求检查确认只有 Python 入口，所有开发请求使用约定代理前缀。

## 依赖与并行

与 B01 并行；独占 frontend/。容器及环境入口由 B03 在本 issue 完成后统一。

依赖以文件头的 `depends_on` 为准；共享路径是计划写入边界，开始前按当前源码复核。执行规则见[工作索引](../../../README.md)。

## 完成记录

负责人：`spec_api_ui` 子任务；2026-09-16 开始执行原样导入和基线构建。

- 已阅读当前 AGENTS、总计划、执行索引、S01 spec 和本 issue，frontend/ 由本任务独占。
- 已从固定 Git 对象导入 10 个必要文件，逐个原始 blob 指纹一致；源工作区保持干净。独立部署文件未导入，供 B03 统一接入。
- Node `v22.20.0` / npm `11.11.1` 下 `npm --prefix frontend ci` 与未改基准 `npm --prefix frontend run build` 均成功；依赖和产物已确认被忽略。
- 当前工作树索引建立后完成上游影响分析，说明共享请求与设置的 CRITICAL 范围；符号 rename dry-run 已审查，UNKNOWN 项用源码引用补核。
- 完成 MediPet 品牌、单一 Python 配置、运行时键及开发代理；移除 Java 切换和健康回退，保留既有布局与请求方法签名。依赖数据与固定对象深比较，仅包名变化。
- 调整后构建通过；`node --test frontend/tests/python-backend.test.mjs` 5/5 通过，覆盖 11 个请求方法、实际组件健康失败和 Vite 到本地替身的转发。业务/真实后端/容器验收仍由后续 issue 执行。
- B03 生成 `window.__MEDIPET_CONFIG__ = { pythonApiUrl: '/api/python' }`；开发代理上游变量为 `MEDIPET_PYTHON_API_URL`。已交还 frontend 范围。
- 来源、影响分析、验证及交接记录见[前端来源对照](../../../frontend-source-map.md)。2026-09-16 标为 `done`，未提交或推送。
