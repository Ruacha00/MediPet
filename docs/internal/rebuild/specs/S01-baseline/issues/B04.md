---
id: B04
spec: S01
title: 验收基准机制并建立工作树索引
status: done
milestone: P1
depends_on: ["B03"]
touches: ["tests/test_agent_orchestrator.py","docs/internal/rebuild/baseline-validation.md"]
---

# B04 验收基准机制并建立工作树索引

## 目标

形成可以开始并行场景适配的已验证基准。

## 实施范围

- 运行原机制假模型回归，覆盖角色契约、路由、工具白名单、三次工具循环、并行及整合降级。
- 核对启动、健康检查和前端构建证据，记录固定依赖及尚未开始的业务能力。
- 刷新当前工作树 GitNexus 索引，明确路径、当前提交与分析边界；索引产物保持忽略。

## 输入与输出

输入：B03 可运行基准。输出：内部基准验证记录；这是 H01 及场景实现的共同前置。

## 验收

- [x] 假模型机制回归、后端启动/健康检查和前端构建均通过；未通过项必须解决后关闭。
- [x] 当前图谱指向 MediPet-Rebuild，包含新导入代码；原工作区索引不充当本工作树的验证。
- [x] 记录分别标明源码核对、假模型、真实存储和实际模型的执行情况；没有将假模型通过写成业务上线。

## 依赖与并行

在 B03 后执行，验收期间冻结基准入口；完成后按依赖表分派场景工作。

依赖以文件头的 `depends_on` 为准；共享路径是计划写入边界，开始前按当前源码复核。执行规则见[工作索引](../../../README.md)。

## 完成记录

主 agent 完成集成验收。12 项假模型测试、前端构建、四容器健康检查和代理已通过；当前工作树索引完整重建成功，1427 nodes / 2596 edges / 91 flows，FTS available 且实际查询成功。详见[基准验证记录](../../../baseline-validation.md)。P1 已通过，可进入 H01；不代表业务实现完成。

假模型机制回归（2026-09-16，`/root/spec_agents_knowledge`）：核对原 10 项测试，已有工具往返及独立整合器降级测试，但没有直接断言三次模型请求上限或调用 `run_parallel` 证明并行。仅在 `tests/test_agent_orchestrator.py` 新增两项：连续工具请求在第 3 次后停止并返回失败；两个真实角色 Agent 使用事件屏障同时进入假模型调用，释放后验证真实整合器请求失败仍保留主辅内容和身份。超时仅防止死锁，不以耗时判断并行。执行 `.venv/Scripts/python.exe -m pytest -q tests/test_agent_orchestrator.py`，结果 **12 passed in 0.92s**。未调用真实模型或存储，未修改生产源码；工具成功分支的 trace 修复仍由 A04 承担。
