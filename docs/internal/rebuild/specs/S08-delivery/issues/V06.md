---
id: V06
spec: S08
title: 执行实际模型演示并生成首次基线
status: in_progress
owner: /root/spec_api_ui
milestone: P6
depends_on: ["V02","V04","V05"]
touches: ["tests/browser/run_evaluation.py","evaluation/reports/**","docs/internal/rebuild/evidence/live-model.md"]
---

# V06 执行实际模型演示并生成首次基线

## 目标

以实际模型完成固定场景并保存可复核的首份 MediPet 评测基线。

## 实施范围

- 记录模型、实际启用的向量实现、参数、评测集与版本，执行固定演示及场景评测。
- 保存完整成功/失败结果、Judge 不可用情况和人工复核结论；不预先承诺准确率。
- 使用与 API 兼容的本地模型配置；缺少访问配置时明确记录受阻，不能以假模型完成关闭。

## 输入与输出

输入：V02 可运行评测器、V04/V05 已验收产品。输出：实际模型报告、演示记录和人工接受的基线。

## 验收

- [x] 真实模型完成医院查询、复合协作、预约确认/查询/取消及患者切换演示，业务状态有对应记录。
- [x] 完整评测结果与失败样本保存；人工复核前不覆盖已接受基线。
- [x] 未声明未经同条件前后对照测得的提升数字；报告明确区分实际模型与确定性测试。
- [ ] 用户人工复核并明确接受候选，保存带复核人的首次基线（对应本 issue 输出与 S08 完成标准）。

## 依赖与并行

共享模型/演示环境由本 issue 使用，正式运行时冻结数据集和已验收代码；文档 V07 可在此前并行草拟。

依赖以文件头的 `depends_on` 为准；共享路径是计划写入边界，开始前按当前源码复核。执行规则见[工作索引](../../../README.md)。

## 完成记录

- 2026-09-16，`/root/spec_api_ui` 接取完整真实模型评测。当前仅准备，不执行模型请求；正式单次运行须由主 agent 在 V04/V05 与 Skills 还原完成、业务代码冻结后明确通知。V08 文档范围已交回主 agent。
- 新增 `tests/browser/run_evaluation.py` 与 `evaluation/reports/README.md`。脚本默认只检查 54/12/12 输入及配置；`--execute` 从实际 5173 页面点击完整评测一次，经现有代理连接 8010，最多等待 1800 秒。保存原始 JSON、完整失败摘要、页面截图/HTML/文字、耗时、源码前后指纹、输入与非密钥参数；不更换答案、不重试、不接受基线。
- 入口影响核对：GitNexus 绑定 `D:/Projects/Agent Learn/Project/MediPet-Rebuild`，索引 `2026-09-16T07:47:37.679Z` / `d0d2bdb`（2639 nodes / 6176 edges）。`run_eval` 上游 UNKNOWN；已从当前源码核对页面 `runEvaluation` → `frontend/src/lib/backends.js` 无 body 的 `POST /eval/run` → API 默认三组数据 → `EndToEndEvaluator.run` 顺序执行。仅增加外部浏览器消费脚本，不修改任何生产函数、接口、数据集或配置；已按主 agent 要求停止图读取，未建图。
- 准备检查：bundled Python 运行默认预检输出 54/12/12、62 个运行源文件、0 次真实请求；脚本编译与 `git diff --check` 通过，Chromium 1208 可用。主 agent 已明确确认 U05/V04/V05 done、代码/Skills/cases 冻结及开发 API 的 `deepseek-flash` / disabled 配置。
- 正式单次运行于 2026-09-16 16:19:16（UTC+8）从 `http://127.0.0.1:5173` 页面点击，工具 session `8768` 已退出 0，输出目录 `evaluation/reports/live-20260916-full`。命令：bundled Python 执行 `tests/browser/run_evaluation.py --execute --url http://127.0.0.1:5173 --output evaluation/reports/live-20260916-full --timeout-seconds 1800`。HTTP 200 响应耗时 269.062 秒，总 269.812 秒；页面 0 错误、1 次请求、62 个源文件前后指纹一致、接受基线前后均不存在。
- 首份候选 run_id `0f457b4d311941aab9ad63a16dad216d`：输入 54/12/12；意图 54/54、Accuracy/Macro-F1 1.0；原始结果 39/49 通过，24 个有效质量项中 9 个低于阈值，24 业务组中 `collaboration` 未通过同轮 Guidance/材料卡断言。调用/Judge/跳过原始汇总均 0。完整原文、截图、失败摘要、参数、源/输入指纹及全部 10 未通过项见[首份候选说明](../../../../../../evaluation/reports/live-20260916-full/notes.md)。
- 审查发现 `_turn(judge=False)` 提前返回漏掉失败 trace、知识来源计数误用 result_count，本候选不得接受为可靠最终基线。协作“需要的资料”缺 Guidance/visit_checklist、路线“大厅”未解析及本次低 Judge 分均原样保留；Nginx 原 180 秒不足以覆盖实测。主 agent 派发有限修正后需用新版本从验证容器入口重新评测，等待明确通知，不自动重试；本 issue 保持进行中，人工复核与最终验收待补。
- 原始证据复核通过：响应 SHA 与 execution 一致、服务候选内容语义完全相同、运行脚本快照 SHA 对应原清单、证据链接存在、密钥模式扫描无命中。新增 `--expected-api`（默认 8010）供下一轮明确记录 18088→18000；原始响应/摘要/截图不修改。脚本新增说明宿主 baseline 指纹不代表容器卷，下一轮由主 agent 补容器 baseline 前后证据。
- 审查进一步确认 Judge 背景缺本轮真实工具事实/卡片/时钟，由评测器负责人修正输入而不改评分算法。路由“资料”同义表达补修及 205 项关联替身回归已交回主 agent，见 A02；本次原始失败分数与 trace 完整保留。`--expected-api` 预检及脚本编译通过，下一轮仍待容器重建后的明确通知。
- 第二次运行已获主 agent 明确启动通知：路由/评测修正交回、业务代码/Skills/cases 冻结，验证容器重建健康，agents/evaluator SHA 与当前树相同，模型/disabled/兼容地址已核对，容器 baseline 运行前不存在（`.scratch/V06-container-before.json`，运行后由主 agent 复核）。于 2026-09-16 16:34:48（UTC+8）从 `http://127.0.0.1:18088` 页面单次点击，`--expected-api http://127.0.0.1:18000 --output evaluation/reports/live-20260916-container --timeout-seconds 1800`，session `48553`；当前等待真实响应。代码和 Judge 背景已变化，不把两次分数差异写成严格同条件质量提升。
- 第二次 session `48553` 已退出 0，运行编号 `a13acc91e787406bb909067a456be4a3`。HTTP 200 响应 170.344 秒，总 170.844 秒，1 次页面请求、0 页面错误、62 源文件指纹期间不变。默认54/12/12，意图54/54，24有效质量项与24业务组均通过，总49/49；Judge/调用失败/跳过/业务断言失败均0。实际仍保留“大厅”地点 not_found 业务 trace，报告不把它改写为成功。完整证据见[容器候选](../../../../../../evaluation/reports/live-20260916-container/notes.md)和[真实模型交接](../../../evidence/live-model.md)。
- 两份候选均未接受，首份原始结果未变。主 agent 已补容器前后证据：baseline均null，容器候选与HTTP响应规范化JSON SHA相同（`7712e4f5b85e2c9b0472234f52bb4ea9c979359586df9fa1274aeee66600b07d`），run_id/status/accepted_by核对一致；文件已归入第二份报告目录。当前只待用户人工复核；容器本次请求未超过旧180秒，仅证明新代理完成本次完整调用，不夸大超时验证。V06维持 `in_progress`，不因49/49自动关闭或接受基线。
