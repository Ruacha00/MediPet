---
id: V03
spec: S08
title: 验证真实 Redis 与 Chroma 数据语义
status: done
owner: /root/audit_preparation_scope
milestone: P6
depends_on: ["H06","M04","K02"]
touches: ["tests/integration/test_storage.py","docs/internal/rebuild/evidence/storage.md"]
---

# V03 验证真实 Redis 与 Chroma 数据语义

## 目标

用真实组件证明事务、持久化与患者过滤行为。

## 实施范围

- 在 MediPet 专用测试前缀/集合运行预约确认并发、重复确认、取消及库存冲突测试。
- 验证完整消息无工作记忆 TTL、记忆过期可恢复、情景及画像按患者过滤。
- 验证知识初始化重复运行不会重复导入，现有业务数据不被普通初始化覆盖。

## 输入与输出

输入：H06 事务、M04 恢复、K02 知识初始化和 B03 存储环境。输出：真实存储集成测试与可复核记录。

## 验收

- [x] 实际 Redis 中同方案重复确认只得到同一记录，冲突不造成负库存或半写入。
- [x] 实际 Chroma 查询不串患者，工作记忆压缩/过期后完整历史仍在且可恢复。
- [x] 记录服务版本、运行方式、结果和失败；内存替身结果不替代本项证据。

## 依赖与并行

与 UI 开发、I04 可并行；使用独立测试数据前缀/集合，不能与 V05 共用被重置的数据。

依赖以文件头的 `depends_on` 为准；共享路径是计划写入边界，开始前按当前源码复核。执行规则见[工作索引](../../../README.md)。

## 完成记录

2026-09-16，负责人 `/root/audit_preparation_scope`。新增真实组件集成测试位于 `tests/integration/test_storage.py`，联合现有预约、事项与记忆真实参数完成验证。修正空知识目录的测试设置，改用独立集合初始化后清空验证空结果；默认知识补齐按来源选择片段。未修改生产模块。

定向命令与版本详见[真实存储证据](../../../evidence/storage.md)：19 passed、1 skipped、29 deselected，其中 18 项为真实存储通过，1 项因名称筛选命中而使用替身；归档竞态注入的真实参数按设计跳过，单独列明。真实 Redis 为 7.4.11，Chroma 服务/客户端均为 0.5.23。随机测试前缀与集合已清理，收尾残留均为 0。

本项完成真实存储语义验收；摘要/画像模型仍用确定性替身，容器重建持久化由 V05、实际模型由 V06 验收。GitNexus 对新增测试函数返回未索引的 UNKNOWN，已结合当前源码引用核对；统一索引及提交检查交主 agent。
