---
id: V05
spec: S08
title: 验证容器持久化并提供定向演示重置
status: done
owner: /root/audit_preparation_scope
milestone: P6
depends_on: ["U05","H06","M04","K02"]
touches: ["docker-compose.yml","frontend/nginx.conf","scripts/reset_demo.py","tests/test_demo_reset.py","docs/internal/rebuild/evidence/runtime.md"]
---

# V05 验证容器持久化并提供定向演示重置

## 目标

确保同一启动路径可重复演示，重建不丢数据，重置只清理本项目演示数据。

## 实施范围

- 沿用 B03 配置完成初次启动、第二次启动、容器重建及前端反向代理检查。
- 提供单独开发重置入口，只处理约定的 medipet: 前缀与专用 Chroma 集合；初始化仅补缺失数据。
- 记录最终端口/环境参数/模型兼容地址和可选监控的启动方式，供 V08 写启动文档。

## 输入与输出

输入：前端完整请求和业务存储；输出：稳定容器运行证据及独立定向重置操作。

## 验收

- [x] 初次初始化后创建预约和历史；二次启动及重建后可读取相同记录，代理路径正确。
- [x] 重置完成后演示数据恢复；在同实例放置的非 MediPet 测试键/集合仍保留，禁止全库清空。
- [x] 未增加第二套启动流程，真实密钥不在配置或证据中，外部告警仍为可选。

## 依赖与并行

运行配置在 B03 后由本 issue 独占修改；V03/V04 使用隔离环境，否则排队，避免重置干扰。

依赖以文件头的 `depends_on` 为准；共享路径是计划写入边界，开始前按当前源码复核。执行规则见[工作索引](../../../README.md)。

## 完成记录

- 2026-09-16，`/root/audit_preparation_scope`。主 agent 确认 U05 生产代码/构建已冻结交回后，使用同一 Compose 配置构建独立 `medipet-validation` 项目；四端口为 16379/18001/18000/18088，卷和网络均独立。未改 docker-compose.yml、Dockerfile、前端 Dockerfile/nginx.conf，也未操作既有项目或开发端口。
- 新增 `scripts/reset_demo.py`，默认只预览，显式 `--execute` 删除 `medipet:` Redis 键及三个精确 Chroma 集合，正常 API 启动恢复默认数据。不删除其他键、相近前缀集合、评测候选/基线文件或卷；连接信息与密钥不输出。新增 `tests/test_demo_reset.py` 四项替身检查全部通过（0.76 秒，`.scratch/V05-reset-tests.xml`）。
- 真实验收全部通过：初次创建预约和两条确认历史；二次应用启动原记录和库存不变；四容器 force-recreate 后 ID 全变、挂载不变、预约/回执/完整历史/两份 Chroma 记忆标记及 API 数据卷文件保留。首页、runtime-config 和健康接口 HTTP200，创建/确认/消息读取实际经过前端同源代理。
- 重置实删 51 个项目键和 3 集合，重复执行删 0；两外来 Redis 键、两外来 Chroma 集合及候选目录测试文件保留。随后普通启动恢复两预置患者、17知识片段、原库存5，事项/预约和情景画像记录归零。没有全库清空、删除卷或真实模型请求。
- 修改前分析显式绑定当前工作树。新脚本 UNKNOWN，源码确认无现有消费者；相关初始化方法 LOW/下界与 HIGH 已说明并源码补核，未修改这些方法。实际命令、版本、清理边界、配置/密钥处理和原始证据位置见 [runtime.md](../../../evidence/runtime.md)。
- 交接状态：本项三条验收已通过；收到 U05 负责人直接完成通知并核对其 `done` 状态后关闭本项。U05 零样本真实 EvalReport 接线不调用模型，证据见 [management.md](../../../evidence/management.md)。完整模型评测的 Nginx 180 秒超时属于 V06 实测待核对点，未提前宣称通过。独立四容器交回时健康，卷保留。
