---
id: U002-05
status: done
owner: spec_api_ui
spec: S04-delivery
depends_on: [U002-02]
touches:
  - ".env.example"
  - "Dockerfile"
  - "docker-compose.yml"
  - ".dockerignore"
  - "scripts/prepare_intent_embedding.py"
  - "tests/test_intent_embedding_runtime.py"
---
# U002-05 接入模型准备与离线容器运行

输入：02模型清单、导出入口和编码器；契约配置名。

输出：可复现模型准备、容器模型资产及只读配置，Windows/Linux离线加载证据。

所属 [spec](../specs/S04-delivery.md)，共享行为以[契约](../contracts.md)为准，文件所有权与调度见[计划](../plan.md)。代码修改前补本 issue 涉及符号的 GitNexus 上游分析。

## 验收

- [x] 准备阶段下载/校验，运行请求零下载；生产镜像不包含构建用PyTorch环境。
- [x] 本地与容器模型路径/权限可用，依赖不破坏Chroma；缺失/损坏有明确状态。
- [x] 现有业务数据卷保留，构建/离线检查与准备失败的退出码真实记录。

## 完成记录

2026-09-17，spec_api_ui 完成。当前树 GitNexus 对 Dockerfile/compose 上游均 LOW、无 partial/truncated；图主要覆盖文档引用，已补核 Compose build、api-data 挂载和 EmbeddingConfig.from_env 的五项配置读取。新准备命令只处理固定源下载/离线导出/严格校验，不修改编码器算法。

默认测试 12 passed/1 opt-in skipped；显式真实模型测试 13 passed。Windows 本地复用 11 份校验源，新的 export 入口再次离线导出 22 条数值对齐，空间与冻结资产相同。缺模型 CLI exit 1。独立生产镜像构建与 pip check 成功；真实 Linux 容器 uid1000、network none、只读 rootfs、空 /app/data 挂载下 semantic_ready；2CPU/2GB，冷初始化334.81ms，30次p95 79.09ms，峰值RSS323.18MiB（含MiniLM）。无torch/transformers/.env/.scratch，Chroma原MiniLM仍384维；缺失/损坏分别明确hash_fallback。保留2线程/2000ms初值。

完整禁止构建网络的额外检查因现有apt OCR包安装失败（步骤100/命令1）；首次pip/apt构建仍需网络，主agent明确本issue验收为准备后离线运行，不扩展离线基础镜像流程。两套既有8容器与6卷对象只读可见且仍停止，本任务未启动/清理它们；用户自行factory reset后的内容完整性不能据此证明。

实现、命令、限制、JSON/源码指纹及可复现探针见 [运行证据](../evidence/runtime/implementation.md)，Docker恢复归因见 [记录](../evidence/runtime/docker-recovery.md)。本次没有付费模型/留出集/基线接受或未退出后台进程。09须等待08并以届时冻结源码重新构建业务镜像。
