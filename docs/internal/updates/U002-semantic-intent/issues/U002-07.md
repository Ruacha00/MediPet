---
id: U002-07
status: done
owner: audit_preparation_scope
spec: S03-evaluation
depends_on: [U002-03, U002-06]
touches:
  - "evaluation/semantic_intent.py"
  - "tests/test_semantic_intent_evaluation.py"
---
# U002-07 建立三模式对照与分层报告

输入：03诊断/编码接口、06冻结数据与模板快照；S03报告契约。

输出：hash/semantic/disabled执行器、固定LLM输出重放、排名/融合/选择性准确率及性能报告。

所属 [spec](../specs/S03-evaluation.md)，共享行为以[契约](../contracts.md)为准，文件所有权与调度见[计划](../plan.md)。代码修改前补本 issue 涉及符号的 GitNexus 上游分析。

## 验收

- [x] 相同输入、模板、LLM输出及配置可复现；每次新实例，不用learn污染评测。
- [x] Top1/MacroF1/Top3/混淆与接受准确率/覆盖率计算有独立样例测试，OTHER不被隐去。
- [x] 实际后端、失败数、耗时分层、指纹与逐例错误完整；不改写原评测报告。

## 完成记录

2026-09-17：03、06均已完成。新增 `evaluation.semantic_intent` 的rank/collect-llm/replay命令，重用真实recognize、校准与投票方法；报告记录数据/模板/规则/源码/原始请求响应指纹。dev网格每后端最多25组合，固定LLM故障构造显式标为injected_failure；正式holdout需显式授权参数及已审阅manifest，禁止网格使用。

显式当前工作树 `_vote` 上游为HIGH，已向root报告并核对现有recognize/编排/旧评测链；未修改生产模块。独立只读审阅的两项缺口已修：请求前fsync started、请求后completed，不确定中断阻断自动重发；NaN/序列化异常保留可读失败；按实际后端分组，任何降级/分类失败使quality_valid=false。原候选报告不覆盖或自动接受。

`.venv/Scripts/python.exe -m pytest tests/test_semantic_intent_evaluation.py -q --junitxml=docs/internal/updates/U002-semantic-intent/evidence/evaluation/tests.xml`：19 passed，52.98秒。最后仅完善中断时取消并回收并发任务，再定向运行 `-k started_without_completed`：1 passed/18 deselected，3.30秒。临时4条合成输入、真实hash算法和假SDK/语义失败替身；没有正式dev/holdout推理、ONNX或真实DeepSeek调用，没有Redis/Chroma操作。数据准备检查另有8 passed，不将其合为质量成绩。

[交接说明与CLI](../evidence/evaluation/README.md)、[完整JUnit](../evidence/evaluation/tests.xml)、[中断补验](../evidence/evaluation/interruption-tests.xml)、[影响证据](../evidence/evaluation/impact.json)。08仍需真实排名、阈值冻结、一次正式留出和共享真实LLM融合对照；07测试不替代这些验收。
