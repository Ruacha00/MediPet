# U002-08 冻结对照结果：候选未通过

2026-09-17。实际中文ONNX、冻结开发门槛、一次独立留出与真实DeepSeek响应的三模式重放已执行。**故障接受准确率未达到90%，因此08不关闭，默认语义方案不宣称已获验收，09业务容器验收与10正式文档交付不启动。** 不自动接受报告，不提交或推送本轮。

## 对照结果

| 指标 | semantic | hash | disabled |
| --- | --- | --- | --- |
| 留出单句原始Top1 | 95/160（59.375%） | 62/160（38.75%） | 不适用 |
| 留出单路Macro-F1 | 0.456180 | 0.287303 | 不适用 |
| 低字面重合Top1 | 80/132（60.606%） | 52/132（39.394%） | 不适用 |
| 相同真实LLM响应的留出融合 | 185/186（99.462%） | 185/186（99.462%） | 185/186（99.462%） |
| 原66条融合回归 | 66/66 | 66/66 | 65/66 |
| 注入LLM失败后的接受准确率 | 69/84（82.143%） | 18/18（100%） | 6/6（100%） |
| 注入失败覆盖率 | 84/160（52.5%） | 18/160（11.25%） | 6/160（3.75%） |

低重合单路提升21.212个百分点，超过预声明5个百分点；融合结果持平，不能把单路提升写成整套助手准确率提升。语义故障覆盖率达50%，但15条误接收导致准确率门槛失败；哈希/关闭向量的低覆盖也不能冒充默认故障方案达标。具体逐例结果与混淆见JSON，不在本轮按留出错误修改数据、模板或门槛。

开发网格各25组，正常权重.7/.2/.1与最终.5未改，关键词强门槛.75；选定semantic .55/.04、hash .15/.03。配置/模板/模型/数据/生产源码在正式采集前冻结；独立复核见review.md。真实语义排名无降级或编码失败，各模式fusion_quality_valid为true；它仅表示证据有效，不代表效果过关。

性能：开发阶段100次同句暖查询、并发1、线程2，semantic P50/P95 8.82/9.29ms，含模板比较、不含LLM；整进程峰值238.71MiB，不称模型独占内存。另有Linux断网/2CPU/2GB证据见../runtime/implementation.md。

## 执行及恢复记录

1. search-plan.json在dev推理前写入；U002-calibrate.py执行dev真实排名、100次暖查询与显式injected_failure网格，仅用于选故障门槛，不能当真实融合。
2. U002-frozen-compare.py第一次执行完成全部186条holdout捕获及三模式回放后，在额外旧提交比较遇到旧独立recognize未前置急症、因冻结记录按共享固定急症规则跳过LLM而parsed为空的脚本错误。退出1，已有排名/响应/回放全部保留。
3. U002-frozen-compare-v2.py仅对旧提交比较增加与原API一致的固定急症前置，并复用全部已保存holdout文件；未改生产源码、配置、模板、数据，也没有重发holdout请求。随后首次采集原66条，完成三模式与7a4e22a旧分类器的相同响应重放，退出0。语义模式相对旧提交和新哈希对照均未增加原66错误。
4. 旧分类器来自git show 7a4e22a:core/intent_recognizer.py，保存在ignored .scratch/U002-baseline-intent.py；旧模型仍用本地字符哈希，重放未调用额外LLM。其源码SHA在*-legacy-fusion.json，可从历史恢复。
5. 真实采集SDK重试为0、并发最多3、超时60s。started在收费请求前fsync，completed后续落盘；未完成started会阻止自动重发。每份raw文件保留请求/响应/输入指纹，不向模型提供期望标签。

## 后续边界

本次holdout已使用，不能继续调参数并把它当未见数据。下一轮应单独冻结改进范围、开发规则及新的独立留出集，再衡量默认方案；当前失败证据永久保留。已向用户提供“补场景模板并保留轻量模型”与“比较更大中文模型”的路线选择。09隔离存储仅为准备，未启动新API/前端或付费业务演示；正式文档仅有未应用草稿，不冒充完成。
