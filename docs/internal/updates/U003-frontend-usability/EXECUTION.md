# U003 执行检查点

2026-09-18：修复前端 API 文档入口的代理前缀丢失，Swagger/ReDoc 使用相对 schema 地址，OpenAPI servers 使用相对当前 schema 的地址。4 项文档回归及 20 项相关 API 测试通过；本地 API/前端镜像已重建，8088 Docker 入口和 5174 开发预览已恢复。8088 浏览器点击“管理与调试 → API 文档”，接口列表加载及 Try it out 健康检查返回 200 均已确认，见[修复验证](evidence/api-docs-fix.md)。本次运行状态覆盖下文旧的“未替换8088部署”；其他 U002/U003 未验收项不因此变更。

2026-09-17：用户要求“实施”，开发已完成，状态 `implemented_with_validation_limits`。分支 `codex/u003-frontend-usability`，基于 `e81f447`。用户随后要求按原版结构重写根README并提交、推送当前分支；本次交付不替换8088部署。

## 任务状态

01—06 done；07 partial（自动回归和主要浏览器路径通过，三项环境验收未完成）。详情见issues目录、[契约](contracts.md)与[验收证据](evidence/acceptance.md)。

root拥有App.vue、全局样式与集成接线；三个有界agent完成Markdown、事项上传、业务卡片。后续只读审查交回4项缺陷，root修复并补回归。子agent全部交回，无递归派发或活动写入任务。

## 实际验证

- 前端82 passed，生产构建通过；最终日志 .scratch/U003-frontend-tests-final.log 与 U003-build-final.log。
- 隔离ASGI API/Redis/Chroma回归107 passed / 5 skipped；另在禁网只读临时容器对4项OCR补验，4 passed / 0 skipped，分别计数，无实际模型请求。
- 浏览器360/768/1440与720×450重排、抽屉Tab/Esc、草稿/管理返回、宽表格与安全Markdown、隔离夹具预约创建/取消/回执/重命名/归档恢复/患者切换已检查。
- GitNexus当前树索引已更新；2批变更分析完整返回，风险CRITICAL，已核对调用链和实际状态测试；索引固有Vue/跨语言与流程枚举缺口见验收证据，不把UNKNOWN当低风险。

## 保留环境检查

1. Edge扩展文件URL权限阻止自动选文件；键盘可打开选择器，组件/接口/实际OCR通过，但浏览器上传完整链路未验收。
2. 浏览器快捷键未改变真实缩放；720×450仅证明小高度重排，不冒充200%验证。
3. 真机软键盘与安全区域需实机检查。

无需为上述检查重跑完整模型评测。配置浏览器权限或实机环境后只补相关验收。

## 工作区与预览

真实API开发预览 `http://127.0.0.1:5174/`（Vite session72848）保留，浏览器新标签1243626373。原8088/8000/6379/8001运行未改变。临时5175前端已停止，18009夹具已关闭且端口释放；独立存储/OCR容器已移除。

既有教程/简历与U002文件原有修订保留，完整使用指南仅追加U003操作章节并修订评测入口；范围外document-source-manifest.json哈希未变。U002效果门槛未通过、09未验收的状态独立保留。

当前交付范围包含U003源码与证据、此前已完成的U002教程/简历增量以及按原版结构重写的根README；范围外来源清单继续排除。提交前需完成全工作树变更分析、链接/秘密检查和测试状态复核，再推送同名远程分支。不要把提交或构建误报为8088已部署。
