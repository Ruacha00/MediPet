# U003 验收记录

工作树：`D:/Projects/Agent Learn/Project/MediPet-Rebuild`。实施分支 `codex/u003-frontend-usability`，基于 `e81f447`。本记录区分运行层级；不把历史回复、替身接口或页面构建当成新的真实模型质量验证。

## 源码与自动测试

- `cd frontend; node --test tests/*.test.mjs`：82 passed，0 failed/0 skipped（日志 `.scratch/U003-frontend-tests-final.log`）。涵盖受控 Markdown、业务状态、身份与事项隔离、上传、管理按需读取、IME、刷新滚动、草稿恢复、未知确认结果。
- `npm run build`：通过，Vue/Vite 生产产物生成（`.scratch/U003-build-final.log`）。本轮唯一新增运行依赖是固定 `markdown-it@15.0.2`；依赖审计边界见 message-dependencies.json。
- 原有 Python 契约导出测试在本轮首次全量执行时暴露旧断言：7种业务卡片被直接与包含新增3种健康卡片的10种总契约比较。仅调整测试按业务类型筛选，未修改契约或后端行为。
- 审查与浏览器实测发现并修复：抽屉关闭时 inert 尚未解除导致焦点失败；跨事项网络失败漏记未知确认；跨事项发送失败丢草稿；成功核对后旧提示残留；刷新读取失败覆盖未知结果原因；历史加载占位退出前滚底时机错误。

## 后端层级

独立 tmpfs Redis/Chroma + ASGI API 回归：107 passed / 5 skipped / 0 failed；无实际模型调用。命令、真实存储范围、跳过原因及 OCR 后续补验见 [后端记录](backend-regression.md)。后续在禁网、只读临时容器中实际运行 Tesseract，4项OCR补验通过（图片、扫描PDF、中文图片、混合PDF），0跳过；与首批结果单列，不相加冒充同次全量。此数与前端测试不是同一测试套，也不与其他版本指标相加。

## 浏览器可用性

通过 Edge 新标签访问独立 Vite 5174，代理现有8000仅做读取；没有操作已有患者预约或重启8088。历史回复只用于排版，不代表最新模型能力。

| 检查 | 实测 |
| --- | --- |
| 360×800 | 页面宽360，文档宽360，输入区底部800；消息区独立滚动，顶栏身份和切换可见 |
| 768×1024 | 页面宽768，文档宽768，输入区底部约1011；侧栏与对话区均可操作 |
| 1440×900 | 页面宽1440，文档宽1440，输入区底部约883；管理入口位于右上角 |
| 720×450 | 小高度重排无横向溢出，身份、发送和上传可见；这不是浏览器真实200%缩放验收 |
| 抽屉键盘 | 打开聚焦“关闭”；Shift+Tab回到最后的“归档”，Tab回到“关闭”；Esc返回“切换” |
| 管理返回 | 输入合成验收草稿，进入管理再返回内容保留；已手动清除该草稿，没有发送 |
| 管理功能 | 知识检索、导入、Skills重载、监控、评测和API链接均保留；进入评测显示未运行状态，未调用模型 |
| 首次历史加载 | 实际正文渲染后 scrollTop=8120，最大值=8120；不再停在占位阶段的位置 |

浏览器实际200%缩放：工具发送两种快捷键均未改变视口，不能报告通过；已恢复默认视口。真实移动设备软键盘遮挡/安全区域仍需实机核对。

## 影响分析与工作区

当前树已重新建立 GitNexus 索引（4766节点、11406边、339记录流程）。仅U003与前端范围进行临时索引变更分析，2批均 `partial=false, truncated=false`，风险CRITICAL，Git真实索引未改变，分析期间源码未变。详见 change-analysis.json；完整报告保存在 .scratch/U003-final-changes.json。

CRITICAL涉及发送/选号/取消准备、历史读取、身份切换、归档恢复以及组件显示链。直接调用与Vue模板事件已人工核对，以以上集成测试覆盖实际状态流。索引建立时对跨语言字段与全库流程枚举给出缺口提示，因此完整变更报告不等于全库图谱完备，不能把未索引流程判断为不存在。

除总计划/入口检查点与使用指南的U003增量外，起始已有教程、简历、U002状态及范围外来源清单哈希保持一致。本轮未提交、推送或更新8088部署。

## 确定性浏览器业务验证

在独立5175前端代理本轮18009内存HTTP夹具，使用虚构本人事项；这不是实际模型演示或真实医院预约。夹具脚本位于.scratch/u003-fixture-server.mjs，依据版本库契约fixture构造，不进入产品运行路径。

- 浏览器查询→选号→显示“待确认”且尚未预约→点击“确认预约”→“业务办理回执/预约有效”。随后准备取消→“尚未取消/待确认”→点击“确认取消预约”→“预约已取消/已取消”，取消按钮退出；创建、取消各一次独立confirm端点，不通过聊天执行。
- 360宽视口，宽表格自身294px、内容1118px，文档宽仍360；焦点可落到表格，右方向键后scrollLeft=40。
- 原始script/img标签保留为文字，消息DOM中script=0、img=0；javascript链接不是a元素，唯一有效HTTPS链接具备noopener noreferrer。没有把DOM检查写成完整网络抓包结论。
- 浏览器可重命名事项；归档/恢复和患者切换见同轮操作记录。
- Enter可从“上传报告”打开文件选择器；扩展返回Not allowed，无法把合成txt/png交给浏览器，因此浏览器文件实际提交与错误就地展示未验收。已向用户提供Edge扩展文件URL权限设置说明。格式/大小/取消/重复选择的组件行为测试通过，PDF与图片提取另有API与真实OCR证据，二者不替代浏览器上传全链路。

剩余人工环境检查：浏览器真实200%缩放、实际移动软键盘、允许文件URL权限后的上传全链路。其余本轮开发与自动回归已完成；8088仍为原部署，5174用于实际API预览。
