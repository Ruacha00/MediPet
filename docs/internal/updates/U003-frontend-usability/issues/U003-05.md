---
id: U003-05
status: done
depends_on: [U003-01]
---
# 业务与健康卡片

输入、范围与验收以[计划](../plan.md)对应行为准，接线遵循[契约](../contracts.md)。

## 实现

2026-09-17，spec_api_ui 完成并交回。仅修改 `BusinessArtifacts.vue`、`HealthArtifacts.vue`，新增 `frontend/tests/u003-artifacts.test.mjs`；props/events 与载荷不变，没有改 App、全局样式、依赖或旧共享测试。

- 预约卡片继续直接显示患者、科室/医生、日期/时段、医院/地点、费用、状态及明确确认。事项/方案/预约内部编号与创建时间放入默认收起的原生 details；历史卡片全部保留，终态方案只压缩间距。
- 明确区分已执行、过期、替代、取消、无余号。按 root 新增的前端 `proposalStates[id] = 'checking'` 显示“结果待核对”，提示刷新记录查看回执；此状态不能确认，不改变服务端状态枚举。原确认门槛、防重与事件函数未改。
- 健康卡片保留科室理由、缺失信息、药品剂型/用法信息、报告实际数值/单位/原参考区间/判断与来源。单项原文及全部提取文字按需展开，不丢原文；无可比对项目和缺少区间明确提示待核对，不推断正常。
- 局部样式沿用暗色变量，正文主要为16px、辅助文字不低于13px；按钮与details摘要至少44px高，有可见焦点。长编号、长原文允许换行；窄屏布局仍由root统一集成验证。来源链接保留HTTP(S)限制和新窗口隔离。

## 影响分析

仓库/工作树明确绑定 `D:/Projects/Agent Learn/Project/MediPet-Rebuild`，索引为当前HEAD `e81f447`（4499节点/10367边）。`proposalStatus` 上游LOW，直接调用方为 `confirm`；`confirm`、`select` 与 Health `sourceUrl` 因Vue模板事件/读取缺图为UNKNOWN，未当作无影响。已源码核对组件模板、App第98—101行props/events接线、proposalStates生成与既有组件测试。Health文件路径目标查询未找到，改查其限定路径符号并补源码；不重建索引。新增仅展示字段与文案，原执行函数签名不变。

## 验收

- [x] `node --test tests/u003-artifacts.test.mjs`：12 passed。真实编译并挂载Vue组件，覆盖确认只发一次、权威executed回写、挂载期间过期、checking/替代/取消后的旧回调拒绝、busy/disabled、显式取消、稳定选号载荷及库存变0、历史卡片保留、编号折叠、报告值/flag/原文、来源链接安全与响应式切换。
- [x] `node --test tests/business-components.test.mjs`：8 passed，原七类业务卡片、三类健康卡片及旧事件契约兼容。
- [x] `npm run build` 成功，41模块；`git diff --check` 对两组件通过（仅已有Git换行提示）。
- [x] 没有后端/模型请求、部署、提交或推送。此证据为组件交互与构建验证，不替代U003-06接线和07真实浏览器/键盘/目标尺寸验收。

交接：root按操作目标传入error，并维护checking→executed等权威状态回读；本组件不创建全局错误或另发接口请求。两组件与独立测试已冻结，可进入06。
