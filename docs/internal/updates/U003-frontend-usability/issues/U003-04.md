---
id: U003-04
status: done
depends_on: [U003-01]
---
# 事项与上传

输入、范围与验收以[计划](../plan.md)对应行为准，接线遵循[契约](../contracts.md)。

## 完成记录

2026-09-17，负责人：`audit_preparation_scope`。前置 U003-01 基准和组件接线契约已读，按本轮授权实施，源码与测试交回后冻结。

- [PatientVisitPanel.vue](../../../../../frontend/src/components/PatientVisitPanel.vue)：压缩患者/当前事项摘要和列表间距，移除组件内重复演示装饰；当前项有文字与边框，并保留 `aria-current`、时间及归档状态。各控件保持原事件和真实 ID，重命名自动聚焦输入、修剪首尾空白、拒绝空标题与忙碌提交；错误保留草稿，仅服务端更新后关闭。取消/Esc、患者切换及归档列表切换清理本地编辑状态。
- [ReportUpload.vue](../../../../../frontend/src/components/ReportUpload.vue)：原生可聚焦按钮触发文件 input，禁用/忙碌时两条路径均阻断；上传仍为 `upload(File)`。保留支持格式与 10 MiB 上限校验、同文件重选能力和就地错误，明确 PDF 最多 10 页由服务端校验。忙碌只显示真实状态，无百分比或假成功。
- 所有 props/events 未变；只改组件局部 CSS，辅助文字至少 13px，按钮/选择器/编辑框至少 44px，沿用暗色变量并增加清晰焦点。移除列表自身的窄屏固定高度/滚动，由 root 的工作区/抽屉统一管理滚动。

## 影响分析与验证

GitNexus 显式绑定 `D:/Projects/Agent Learn/Project/MediPet-Rebuild`，索引提交 `e81f447`。`PatientVisitPanel.edit` 上游为 UNKNOWN，`rename` 为 LOW（14项，主要为组件文件与文档引用），`ReportUpload.choose` 为 LOW（3项）。已结合模板及 `App.vue` 的 `selectPatient`、`selectVisit`、`changeVisit`、`submitReport` 核实动态事件链；没有把图谱零调用当成无影响。未重建索引。

- `cd frontend; node --test tests/u003-workspace-components.test.mjs`：**10 passed**。使用编译后的 Vue 模板及现有自定义 renderer 模式，实际触发事件、更新响应式 props、观察焦点与状态；覆盖患者绑定、选择与全部事项事件、保存失败/成功、空标题/忙碌阻断、取消/Esc、原生上传按钮到 input 的触发、格式/大小/取消选择/重选/忙碌恢复。
- `node --test --test-name-pattern="patient and visit|loading and errors|report picker|both standalone" tests/business-components.test.mjs`：**4 passed**，包含既有事件/错误/上传兼容与组件局部样式构建检查。未修改旧测试。
- 所属文件 `git diff --check` 通过。未修改 App、全局样式、API 或依赖，未发送真实上传、业务操作或模型请求。

上述为组件替身与编译验证，不是实际文件选择器、浏览器布局、服务端 OCR 或真实业务验收。Tab/Enter/Space、抽屉焦点、360/768/1440尺寸与真实报告链路由 U003-06/07 在最终接线后统一检查。
