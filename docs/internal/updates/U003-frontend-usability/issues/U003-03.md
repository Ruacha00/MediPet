---
id: U003-03
status: done
depends_on: [U003-01]
---
# 受控消息排版

输入、范围与验收以[计划](../plan.md)对应行为准，接线遵循[契约](../contracts.md)。

## 交付

- 新增 `frontend/src/components/MessageContent.vue`，只读必填 `content: string`，无事件。root 在 App 接入 `<MessageContent :content="item.content" />`；未修改 App、全局样式或旧共享测试。
- 固定 `markdown-it@15.0.2`，支持段落、显式换行、嵌套列表、强调、表格、安全链接及代码文字；关闭原始 HTML、自动链接和排版替换，不截断或改写消息事实。
- 仅允许显式 HTTP/HTTPS 链接，`target="_blank"` 配合 `noopener noreferrer`；危险协议、相对地址及其他协议保留为不可执行文字。所有 Markdown 图片只保留转义后的说明与“未加载”标记，不产生图片节点。原始 HTML 和代码中的标签按文字显示。
- `v-html` 仅接收关闭 HTML 后解析器生成的结果；没有 HTML 插件，定制图片内容显式转义。表格被自身可横向滚动且可聚焦的 region 包围；组件局部样式处理长标识、代码及焦点，不扩大页面滚动范围。

## 验证与证据

- GitNexus 已绑定当前绝对工作树 `D:/Projects/Agent Learn/Project/MediPet-Rebuild`、索引 `e81f447`；依赖入口 `frontend/package.json` 查询为 UNKNOWN（没有 JSON 符号），已补查 `main.js → App.vue`、Vite 及 Docker 的 `npm ci` 构建入口。新组件无既有调用方；没有改动既有运行符号或依赖版本，App 接线与最终变更分析由 root 处理。
- 官方资料核对：[markdown-it 用法](https://markdown-it.github.io/markdown-it/documents/Usage_examples.html)、[HTML 等解析选项](https://markdown-it.github.io/markdown-it/interfaces/MarkdownItOptions.html)。定制 renderer 的转义与 token 输出另核对已安装版本源码。
- `node --test tests/message-content.test.mjs`：**17/17 通过**。实际 Vue SSR 编译与渲染验证格式、表格结构、安全链接、10 类非允许地址、原始脚本/事件/SVG/iframe、远程与 data 图片、属性引号、代码块、长原文及实例间内容隔离。首轮一条断言误将已转义的 `src=` 文字当成节点属性，修正为检查真实标签后通过；没有为通过测试放宽安全策略。
- `npm run build`：通过。这里只是当时共享工作树的构建记录，不能替代后续 App 接线/浏览器验收。
- [依赖锁核对](../evidence/message-dependencies.json)：只增加 markdown-it 及 6 个传递包；既有锁定条目均未修改。npm audit 的 3 项告警来自既有 esbuild/nanoid/postcss（1 low、2 high），新增依赖链无告警；没有扩展到工具链升级。
- 浏览器交 root 的 U003-07：360/768/1440 与缩放下整页无横溢，长表格只在 region 内滚动并可键盘聚焦；危险内容无执行或资源请求；更新 content 后替换当前显示；结合 App 验证只读文字不能触发预约确认。此项尚未由本任务运行，无真实 API、模型或用户数据操作。
