---
id: U001-10
status: done
owner: spec_api_ui
depends_on: [U001-09]
---
# U001-10 报告API与历史

范围与文件所有权见[计划](../plan.md)，输入/输出见[契约](../contracts.md)。

验收：患者及事项身份、归档、错误和历史保存验证通过，不改变预约方案。

## 完成记录

2026-09-16，spec_api_ui 完成 `POST /reports/preprocess`：multipart 接收文件及身份字段，复用 `_load_visit`；用户、患者、事项或归档校验失败时不处理上传。提取结果以普通聊天消息和 `report_summary` 卡片写入同事项完整历史，元数据为 `processing=report_preprocessor`，只失效工作记忆，不覆盖预约选择。写历史时再次核对归档状态。

与 U001-08/09 共用的测试最初 36 项通过，补充混合 PDF 回归后 **39 项全部通过**。接口验证使用隔离内存存储：覆盖身份拒绝、归档、处理期间归档、错误不写历史、大小上限及卡片与历史一致；另有真实 PDF 子进程经过 HTTP API，核对实际 `6.2` 与 `within`。选择状态前后相同。详见[报告验证记录](../evidence/reports.md)。真实 Redis、容器与浏览器不是本项替身测试的结论，由 U001-13 验收。
