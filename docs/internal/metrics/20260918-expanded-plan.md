# 简历效果指标扩测

用户要求继续扩大、优化测试，并严格按原版简历的“技术实现＋业务作用＋效果指标”组织正文。上一轮原始报告保留。

## 固定工作量与口径

- 80个业务任务：预约创建20、取消20、复合咨询20、上下文衔接20。覆盖内科/儿科/眼科、本人/家属、未来多个日期、首项/第二项、日期与时段修正、改选、窗口恢复，不以完全重复请求凑样本。
- 40条知识检索问题：覆盖24份内置资料，执行前由本次评测任务按文档内容指定目标文档ID；包含多文档需求。比较直接向量Top-3与当前改写/并行召回/重排Top-3，计算文档级宏平均Recall@3及Hit@3。标签不会送给模型，不声称经过外部人工标注或独立复审。
- 业务成功按用户目标对应的工具结果、卡片字段、确认回执和库存判断；预约创建与取消分别统计。复合任务以首轮正确号源与非空材料清单为交付标准，同时另报双角色覆盖率，不强迫把内部角色数量等同用户任务完成。
- 上下文衔接按最终科室、日期、时段、选择目标及恢复状态检查，调用失败、缺失结果保留在分母。
- 每个业务任务和检索问题只运行一次；保存输入和代码指纹、模型配置、原始输出及全部失败。不给LLM看期望答案。未经用户要求，不调产品源码、重跑择优或接受基线。
- 使用现有deepseek-flash和Docker真实存储；评测服务按随机前缀隔离。RAG用单独临时Chroma集合载入固定24份文档，结束只删除自身集合；不接触在线导入资料。
- 新增套件为自建业务评测，不宣称随机线上流量或未知盲测；比例可写入简历但须简短注明“自建评测集”。详细分母与限制放证据附件。

执行前追加意图回归：将U002已经使用过的186条固定样本在当前部署代码上重新独立调用模型一次，覆盖18类和上下文/歧义控制组，计算Accuracy与Macro-F1。使用全新识别器、不注入learn样本，标签不进入模型请求。本次明确属于已知集回归，不重新宣称独立留出验证；历史冻结响应对照仍保留，不覆盖。

## 交付写法

保留原版项目背景、技术栈、强指标版、精简投递版及面试说明的章节顺序。正文每条先写做了什么、解决什么问题，再写最有代表性的实测指标；不在每条末尾堆验收编号、待办、调用计数或免责声明。没有数据支撑的收益不补造，未测项无需塞进简历正文。
